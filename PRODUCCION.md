# EduPago — Guía de puesta en producción

## Paleta de colores actualizada
| Token CSS | Hex | Uso |
|---|---|---|
| `--navy` | `#282d65` | Color primario, sidebar, navbar |
| `--lime` | `#bdcf00` | Acento principal, botones, highlights |
| `--green` | `#49af54` | Confirmaciones, éxito, pagos ok |
| `--white` | `#ffffff` | Fondo claro |

## Accesos del sistema (demo)
| Rol | Email | Contraseña |
|---|---|---|
| Super Admin | superadmin@pagalaescuela.mx | SuperAdmin2026! |
| Admin ITM | admin@itm.edu.mx | admin123 |
| Cajero ITM | cajero@itm.edu.mx | cajero123 |
| **Familia García** | garcia.fam@mail.com | familia123 |
| **Familia Hernández** | hernandez.t@mail.com | familia123 |
| **Familia Méndez** | mendez.c@mail.com | familia123 |

## Checklist antes de subir a Hostinger

### 1. `config.php`
- [ ] Cambiar `SPEI_CLABE_FIJA` por la CLABE real de STP/Pagadetodo
- [ ] Cambiar `PDT_USER`, `PDT_PASS`, `PDT_INT_ID` por credenciales reales
- [ ] Cambiar `FACTURAPI_KEY` por la llave de producción (quitar `sk_test_`)
- [ ] Cambiar `WEBHOOK_URL` por la URL real del servidor
- [ ] Cambiar `ADMIN_PASS` por contraseña segura

### 2. PHP / Servidor
- PHP 8.0+ con extensiones: `curl`, `json`, `mbstring`
- El directorio debe ser escribible para `api_log.txt`
- HTTPS obligatorio (certificado SSL activo)

### 3. Portal de familias
- Los usuarios con rol `familia` acceden al portal automáticamente al login
- Se vinculan por `familia_id` a sus alumnos
- Crearlos desde Usuarios → rol: Familia → seleccionar familia vinculada

### 4. Webhook SPEI
- Dar la URL `https://tudominio.com/webhooks/webhook_spei.php` a Pagadetodo
- Verificar que el archivo sea accesible sin autenticación

### 4.1 ⚠️ Los 10 endpoints de pago se movieron a `webhooks/` — ACTUALIZAR EN EL SANDBOX
Antes vivían en la raíz del proyecto. Se movieron a una carpeta `webhooks/` para no mezclarlos con el resto del backend. **Esto NO funciona solo — hay que actualizar la URL configurada en el Sandbox de Cobroscontarjeta.com/Pagadetodo para cada uno de los 10**, o ese endpoint empezará a responder 404 en cuanto subas los archivos nuevos.

Secuencia recomendada para no perder notificaciones reales durante el cambio:
1. Sube los archivos nuevos (con la carpeta `webhooks/`) al servidor.
2. Inmediatamente después, entra al Sandbox y actualiza las 10 URLs de la tabla de abajo (agregando `/webhooks/` antes del nombre del archivo — todo lo demás de la URL se queda igual).
3. Haz una prueba real (o de sandbox) de cada método de pago que uses activamente para confirmar que el webhook correspondiente sigue llegando.

| Archivo | Dónde se configura en el Sandbox | URL nueva (con tu dominio real) |
|---|---|---|
| `webhook_spei.php` | Ya cubierto arriba (campo del webhook de Pagadetodo) | `.../webhooks/webhook_spei.php?token=...` |
| `webhook_liga.php` | Pagalaescuela → EntregarPagoLigaToken | `.../webhooks/webhook_liga.php` |
| `pago_referencia.php` | EndPoint → Comercios → "Pagar referencia" | `.../webhooks/pago_referencia.php` |
| `cancela_pago_referencia.php` | EndPoint → Comercios → "Cancelar pago" | `.../webhooks/cancela_pago_referencia.php` |
| `consulta_referencia.php` | EndPoint → Comercios → "Consultar referencia" | `.../webhooks/consulta_referencia.php` |
| `entregar_referencia.php` | EndPoint → Comercios → "Entregar referencia" | `.../webhooks/entregar_referencia.php` |
| `pago_clabe.php` | EndPoint → Pago por SPEI → "Pagar clabe" | `.../webhooks/pago_clabe.php` |
| `cancela_pago_spei.php` | EndPoint → Pago por SPEI → "Cancelar pago" | `.../webhooks/cancela_pago_spei.php` |
| `consulta_clabe.php` | EndPoint → Pago por SPEI → "Consultar clabe" | `.../webhooks/consulta_clabe.php` |
| `entregar_clabe.php` | EndPoint → Pago por SPEI → "Entregar clabe" | `.../webhooks/entregar_clabe.php` |

`config.php` (`WEBHOOK_URL`, `WEBHOOK_LIGA_URL`) ya se actualizó con el prefijo `/webhooks/` — solo son constantes informativas (no se leen en ningún flujo de pago), pero conviene que reflejen la URL real que le das a Pagadetodo.

### 5. Base de datos — migraciones
- Correr `migracion_2026_08_20_suscripciones.sql` una sola vez (phpMyAdmin o consola MySQL de Hostinger) **antes** de subir el `api.php` nuevo — agrega `fecha_vencimiento_plan` / `ultimo_recordatorio_plan` a `escuelas`.
- Correr también `migracion_2026_08_20_pagos_recurrentes.sql` — agrega los campos de recurrencia/penalización a `productos` y `cobros`, y crea la tabla `pagos_recurrentes_generados`. Sin esto, los conceptos recurrentes (Productos → tipo "Recurrente") no se generan ni se penalizan.
- Correr también `migracion_2026_08_20_revocacion_sesiones.sql` — agrega `usuarios.sesion_valida_desde`, usada para poder invalidar tokens ya emitidos (al cambiar contraseña, o al forzar el cierre de sesión de alguien desde Usuarios). Sin esto, `verificar_token_auth()` en api.php tronaría con "columna desconocida".
- Correr también `migracion_2026_08_20_fix_metodo_enum.sql` — corrige un bug real: `cron_recordatorios.php` generaba cobros con `metodo='Pendiente'`, valor que no existía en el ENUM de `cobros.metodo` y MySQL lo silenciaba a `''` (confirmado en producción: cobros #187-192). Agrega `'Pendiente'` al ENUM, corrige los registros ya afectados, y agrega el índice `idx_cobros_estado` que le faltaba a la tabla para no escanearla completa cada vez que el cron busca cobros pendientes.
- Correr también `migracion_2026_08_20_plan_enum.sql` — convierte `escuelas.plan` de texto libre a `ENUM('basico','avanzado','pro')`, para que un typo no pueda dejar a un colegio con un plan inválido (el código ya tenía que defenderse de esto).

### 5.1 Seguridad — pendiente de tu parte (no se puede arreglar solo en código)
- **Hacer privado el repo de GitHub** (`Sangigi/EduPago`) — está público con `config.php` completo expuesto.
- **Rotar `DB_PASS` y `PDT_PASS`** — nunca se rotaron pese a estar expuestos desde la misma fuga que sí motivó rotar otros dos secretos.
- Después de rotar: considera sacar `config.php` de git (`git rm --cached config.php` + `.gitignore`) para que la próxima vez que lo edites no se vuelva a subir con el bot de auto-commit.
- `pago_referencia.php` / `cancela_pago_referencia.php` / `webhook_liga.php`: su protocolo con Cobroscontarjeta.com no admite token, así que la única defensa real es lista blanca de IP — pide la IP real a Cobroscontarjeta.com/Pagadetodo y agrégala en `config.php` → `IPS_PERMITIDAS_PAGOS_SIN_TOKEN` (hoy vacío = sin restricción).

### 5.2 Limpieza de datos de prueba
- Corre `reporte_datos_prueba.sql` (solo `SELECT`, no borra nada) en phpMyAdmin y revisa los resultados — agrupa alumnos/familias/colegios/cobros que parecen ser de las pruebas que hicimos juntos (emails `@example.com`, nombres como "q"/"123", el colegio "Instituto Tecnológico Naulcalpan" de prueba, cobros de $0.01). Con base en eso se genera `limpieza_datos_prueba.sql` con los `DELETE` que confirmes — no se borra nada sin que lo revises primero.

### 5.3 Bug corregido: `saldo_pendiente` desincronizable en pagos SPEI
- `clientes.saldo_pendiente` es una columna cacheada (no se calcula en vivo). Se recalculaba con la misma consulta copiada y pegada en 12 sitios (`api.php` x4, `webhook_liga.php`, `pago_referencia.php`, `cancela_pago_referencia.php`, `cancela_pago_spei.php`, `pago_clabe.php`, `cron_recordatorios.php` x2) — salvo `webhook_spei.php`, que en vez de recalcular desde `cobros` hacía un decremento (`saldo_pendiente - total`). Si ese saldo alguna vez se desincronizaba por cualquier otra razón, la confirmación de un pago SPEI era el único flujo que nunca se autocorregía.
- Se centralizó en `helpers_pagos.php` → `recalcular_saldo_pendiente($pdo, $cliente_id)`, usada ahora en los 12 sitios (incluido el de SPEI, ya corregido). No requiere migración de base de datos — es un fix de lógica, no de esquema.

### 5.3b Hueco de auditoría cerrado: acciones de dinero sin registrar en `logs_sistema`
- `registrar_log()` ya se usaba en 24 sitios (creación de usuarios, escuelas, etc.) pero ninguna de las 7 acciones que mueven dinero de verdad la llamaba: `generar_liga`, `cobrar_cai`, `cancelar_cai`, `generar_referencia_efectivo`, `crear_cobro`, `confirmar_pago`, `cancelar_cobro`. Quedaban fuera del log de auditoría precisamente las acciones más sensibles del sistema.
- Se agregó `registrar_log()` a las 7 (con acciones descriptivas: `liga_pago_generada`, `cargo_automatico_cobrado`, `tarjeta_domiciliada_cancelada`, `referencia_efectivo_generada`, `cobro_creado`, `pago_confirmado_manual`, `cobro_cancelado`). `verificar_spei` se dejó fuera a propósito: es una consulta de solo lectura (no modifica nada), registrarla solo generaría ruido en `logs_sistema` sin valor de auditoría.
- No requiere migración SQL (la tabla y la función ya existían).

### 5.3c Deduplicado: check "admin no puede tocar un usuario de otra escuela ni a un superadmin"
- Este check estaba copiado casi idéntico en 4 acciones (`editar_usuario`, `toggle_usuario`, `cerrar_sesiones_usuario`, `eliminar_usuario`). Se centralizó en `validar_admin_sobre_usuario($pdo, $rol_actual, $usuario_actual, $id, $mensaje)` (api.php, junto a `validar_email_opcional`) — misma lógica exacta, sin cambios de comportamiento.
- No requiere migración SQL.

### 5.3d Frontend: se quitó la mezcla de datos reales con `AppModel` (localStorage)
- `models/AppModel.js` es un store de localStorage de antes de que existiera el backend real. `assets/js/app.js` (función `cargarDatosDesdeAPI`) mezclaba la respuesta real de la API con `AppModel.load()`: si la API regresaba `escuelas`, `productos` o `recordatorios` vacíos, se rellenaba con lo último guardado en localStorage en vez de reflejar el estado real — y ese resultado mezclado se volvía a guardar en `AppModel`, perpetuando datos viejos en pantalla.
- Se quitó la mezcla: ahora `cargarDatosDesdeAPI` siempre confía en la respuesta real de la API, sin caché local de por medio. Los usos de `AppModel.load()` como respaldo cuando la API no responde (sesión 401, error de red, o sin sesión) se dejaron intactos a propósito — no es el mismo problema (es un respaldo ante falla, no una mezcla silenciosa con datos válidos).
- Las ~9 vistas que todavía llaman `AppModel.save(...)` quedaron sin tocar por ahora (son escrituras inofensivas a un store que ya nadie lee para mostrar datos reales) — limpiarlas es la Fase 5 del plan de refactor.
- JS-only, no requiere migración.

### 5.3e Frontend: consolidados los 5 `apiPost()` duplicados en `ApiClient.js`
- `AuthController.js`, `CajaController.js`, `ClienteController.js`, `CobroController.js` y `ProductosController.js` reimplementaban, cada uno, la misma función `apiPost` (fetch + header de auth + logout en 401 + error si HTTP no-ok) — copiada 5 veces casi línea por línea.
- Se centralizó en `assets/js/ApiClient.js` (nuevo, cargado en `index.html` antes de los controllers) → `ApiClient.post(action, body)`. Cada controller ahora solo hace `const apiPost = ApiClient.post;` — ningún call site (`apiPost('crear_cobro', ...)`, etc.) cambió, mismo comportamiento exacto.
- Las 29 llamadas `fetch()` sueltas directamente en las vistas (que no pasan por ningún controller) quedaron sin tocar — es una limpieza aparte, de mayor superficie y riesgo, no incluida en esta pasada.
- JS-only, no requiere migración. Probar: login/logout (ruta 401), y una acción de escritura por cada dominio (caja, cliente, cobro, producto, usuario).

### 5.3f Backend reorganizado en `lib/` + `webhooks/` + logging/respuestas consolidados
- Los archivos internos que nunca se llaman por URL directa (`db.php`, `mailer.php`, `helpers_pagos.php`, y el nuevo `webhook_helpers.php`) se movieron a una carpeta `lib/`. Se actualizaron todos los `require_once` — verificado con `php -l` **y** con una ejecución real por CLI de varios endpoints (no basta con `php -l`: un `require_once` a una ruta que ya no existe es un error que solo aparece en tiempo de ejecución, no de sintaxis — así se encontró y corrigió un bug real: `lib/db.php` tenía su propio `require_once __DIR__ . '/config.php'` interno que quedó roto por el movimiento, lo que habría tronado TODO lo que usa `db.php`, incluyendo `api.php`).
- Los 10 endpoints de pago (`webhook_liga.php`, `webhook_spei.php`, `pago_referencia.php`, `cancela_pago_referencia.php`, `cancela_pago_spei.php`, `pago_clabe.php`, `consulta_referencia.php`, `consulta_clabe.php`, `entregar_referencia.php`, `entregar_clabe.php`) se movieron a una carpeta `webhooks/` — **esto requiere actualizar la URL en el Sandbox de Cobroscontarjeta.com/Pagadetodo para cada uno, ver sección 4.1**, no es automático.
- Además, esos mismos 10 archivos reimplementaban, cada uno, su propia función de "escribe una línea con fecha a un archivo de log" (`log_api_liga`, `log_ref_pago`, `log_ref_cancela`, `log_cancela_spei`, `log_clabe`, `log_ref`, `log_pago_clabe`, o líneas sueltas de `file_put_contents` en `webhook_spei.php`/`webhook_liga.php`). Se centralizó la mecánica de escritura en `lib/webhook_helpers.php` → `webhook_log($archivo, $mensaje)`; cada archivo conserva su propia función con su nombre, su condición de activado y su archivo de destino tal cual estaban (solo delegan la escritura real).
- Los 8 formateadores de respuesta (`responder_liga`, `responder` de SPEI, `responder_pago`, `responder_cancela`, `responder_cancela_spei`, `responder_consulta`, `responder_consulta_clabe`, `responder_pago_clabe`) también repetían el mismo mecanismo (`echo json_encode([...], JSON_UNESCAPED_UNICODE); exit;`). Se centralizó ESE mecanismo en `webhook_helpers.php` → `webhook_responder($payload)` — pero a propósito **no se unificó el contenido del JSON**: cada `responder_*` sigue armando su propio arreglo con los campos exactos que su proveedor/endpoint espera (`codigo`/`autorizacion`/`transaccion`/`fecha` en unos, `success`/`mensaje` en otros). Unificar los CAMPOS habría arriesgado romper el contrato externo con dinero real de por medio; unificar solo el "envíalo y corta" no cambia ni un campo de lo que cada proveedor recibe.
- Efecto secundario menor del movimiento a `webhooks/`: `webhook_liga.php` escribe dos archivos de diagnóstico crudo (`debug_webhook.txt`, `webhook_log.txt`) usando su propia carpeta como referencia — ahora aparecerán dentro de `webhooks/` en vez de en la raíz. Los logs importantes (`api_log.txt`, `referencias_log.txt`) NO se movieron, siguen en la raíz (se definen desde `config.php`, que no cambió de lugar).
- No requiere migración SQL.

### 5.3g Centralizado el cliente de Facturapi.io + corregido `CURLOPT_SSL_VERIFYPEER`
- Las 3 llamadas a Facturapi.io (`generar_cfdi`, `descargar_cfdi`, `enviar_factura_correo` en `api.php`) reimplementaban, cada una, su propio bloque `curl_init`/`curl_setopt_array`/`curl_exec`/`curl_close` — y las 3 tenían `CURLOPT_SSL_VERIFYPEER => false` puesto a mano, a diferencia de `curl_post()` (usado para Pagadetodo/Cobroscontarjeta.com), que ya usa `true`. Un endpoint HTTPS validando certificados en `false` acepta un certificado falso/interceptado sin avisar — real, aunque de menor severidad que los hallazgos de fraude de la auditoría de seguridad de esta sesión.
- Se centralizó en `lib/facturapi.php` → `facturapi_request($ruta, $metodo, $payload)`, con `CURLOPT_SSL_VERIFYPEER => true`.
- ⚠️ **Prueba esto antes de confiar en ello en producción**: pasar de `false` a `true` puede fallar si el bundle de certificados CA del servidor de Hostinger está desactualizado. Genera una factura real de prueba (`generar_cfdi`), descárgala (`descargar_cfdi`, ambos tipos xml/pdf) y envíala por correo (`enviar_factura_correo`) — si alguna falla con un error de SSL/certificado, es el hosting, no el código; en ese caso avísame para volver a `false` temporalmente mientras se resuelve con Hostinger.
- No requiere migración SQL.

### 5.3h Frontend: hook de paginación compartido (`usePaginaActual`) — adopción parcial, a propósito
- Se investigó la duplicación de "adopción amplia de `Badge.js`" del backlog de la Fase 8 y resultó ser un falso positivo: los 57 sitios con clases `badge-*` fuera de `Badge.js` (investigados con 12 agentes, uno por archivo) son badges de rol de usuario, plan de suscripción, activo/inactivo, etapa de embudo de distribuidor, estado de sesión de caja, botones de filtro y contadores de importación CSV — ninguno es un badge de `cobro.estado`/`cobro.metodo`. No había nada que convertir; se eliminó del backlog.
- Se creó `views/hooks/usePaginaActual.js` (cargado en `index.html` junto a los demás componentes compartidos) → `usePaginaActual(totalPaginas)`, que centraliza el estado de página actual + una función `irAPagina` que hace clamp a `[1, totalPaginas]`. A propósito NO calcula `totalPaginas` internamente — cada vista sigue calculándolo como ya lo hacía.
- ⚠️ Se llamaba originalmente `usePaginacion`, pero una actualización posterior del equipo agregó `views/components/Paginador.js` con una función global del MISMO nombre (`usePaginacion`) y firma/propósito totalmente distintos (pagina una lista completa ya cargada, no solo un número de página). En JavaScript no-strict esto no truena — la que cargue después sobreescribe silenciosamente a la otra — así que se detectó revisando el código, no por un error visible. Se renombró la de aquí a `usePaginaActual` para no chocar; `Paginador.js`/`usePaginacion` (de ellos) se dejó intacto, ya está más integrado (`Familias.js`).
- **Adoptado en `Logs.js`** (paginación de servidor simple) y en el historial de pagos de `PortalFamilia.js` (paginación de cliente sobre un arreglo ya cargado) — ambos son coincidencias limpias: solo manejaban número de página + clamp, sin nada más acoplado.
- **A propósito NO adoptado en `Cobros.js` ni `Alumnos.js`**: su `irAPagina` no es solo "cambiar de página" — también dispara un `fetch` al backend (`buscarEnServidor`) para traer esa página. Forzar el hook genérico ahí perdería esa llamada o requeriría un hook más grande y arriesgado ("paginación + fetch"), en las dos vistas de mayor tráfico del sistema, sin pruebas automatizadas. Se dejaron con su lógica original, intacta.
- **A propósito NO adoptado en la paginación por-hijo de `PortalFamilia.js`** (`paginaPends`, un objeto `{ [hijoId]: numeroPagina }`): son N contadores de página independientes según cuántos hijos tenga la familia, un patrón de "mapa de páginas dinámico" que no se puede resolver llamando un hook una vez por hijo dentro de un `.map()` (violaría las reglas de hooks de React). No es el mismo problema que resuelve `usePaginacion`.
- JS-only, no requiere migración. Verificado con `node --check` en los 3 archivos tocados.

### 5.3i Frontend: `ConfirmModal` compartido — adopción parcial, a propósito
- Se creó `views/components/ConfirmModal.js` (mismo `.modal`/`.modal-backdrop` que ya usaba el resto de la app), y se adoptó en `Usuarios.js`, que tenía su propio modal de confirmación armado a mano (para desactivar/eliminar un usuario). El estado (`confirm`/`setConfirm`/`confirmarAccion`) no cambió — solo se reemplazó el bloque de JSX que lo dibujaba por `<ConfirmModal abierto={...} titulo={...} mensaje={...} onConfirmar={...} onCancelar={...}/>`. Cero cambio de comportamiento.
- **A propósito NO se convirtieron** los 8 sitios que usan `confirm()`/`window.confirm()` nativo del navegador (`Cobros.js`, `Escuelas.js` x3, `Familias.js` x2, `Usuarios.js` (cerrar sesiones), `PortalFamilia.js`). El nativo ya funciona correctamente (bloquea y confirma de verdad); migrarlo a un modal de React requiere reestructurar el flujo de control de cada función (de un `if (!confirm(...)) return;` síncrono a un modal asíncrono con estado) — 8 sitios, varios de ellos acciones destructivas reales (eliminar colegio, eliminar plantel, desvincular familia), sin pruebas automatizadas. El beneficio es puramente estético (consistencia visual); el riesgo de que alguna conversión salga mal y una acción destructiva se dispare sin confirmar de verdad no vale ese beneficio. Si en algún momento quieres hacerlo, avísame y lo hacemos uno por uno con cuidado.
- JS-only, no requiere migración. Verificado con `node --check`.

### 5.3j Deduplicados 36 checks de rol puro con `requerir_rol()`
- El patrón "si el rol actual no está en esta lista, corta con 403 y este mensaje" (`if (!in_array($rolX, [...])) { http_response_code(403); respond([...]); }`) estaba repetido **36 veces** en `api.php`, con distintas combinaciones de roles permitidos y distintos mensajes, pero el mismo cuerpo exacto siempre.
- Se centralizó en `requerir_rol($rol_actual, $roles_permitidos, $mensaje)` (junto a `validar_admin_sobre_usuario`). Cada uno de los 36 sitios quedó en una sola línea — mismos roles permitidos, mismo mensaje, mismo comportamiento exacto.
- **A propósito NO se tocaron 3 sitios** que combinan el check de rol con una condición adicional vía `&&` (ej. `!in_array(...) && !$es_propio_perfil`) — esos siguen teniendo lógica extra que `requerir_rol()` no captura, se quedan como estaban.
- **A propósito NO se tocaron los checks de pertenencia** (~30 sitios que verifican que un admin/cajero solo toque filas de su propia escuela, o que una familia solo toque a sus propios hijos) — esos SÍ tienen variaciones reales entre sitios (`!=` vs `!==`, `?? null` vs `?? -1`/`?? -2` como valor de respaldo) que hay que revisar uno por uno antes de unificar, para no cambiar sutilmente el comportamiento de seguridad de ninguno. Queda pendiente para una próxima pasada, con más tiempo dedicado a verificar cada caso.
- Verificado con `php -l`, con una prueba unitaria aislada de `requerir_rol()` (caso permitido sigue ejecutando; caso no permitido corta con 403 y el mensaje exacto), y con una ejecución real de `api.php` por CLI. No requiere migración SQL.

### 5.3k Deduplicados los checks de pertenencia (escuela/familia) restantes
- Se completó la limpieza de checks de autorización: 17 sitios de "solo tu propia escuela" (admin/cajero limitados a su escuela, superadmin exento) se centralizaron en `requerir_escuela_propia($rol_actual, $escuela_id_fila, $usuario_actual, $mensaje)`, y 8 sitios de "solo tus propios hijos" (familia limitada a su propia familia_id) en `requerir_familia_propia($familia_id_fila, $usuario_actual, $mensaje)`.
- Varios sitios originales usaban comparación floja (`!=` sin `intval`, con `?? null` de respaldo) mientras otros ya usaban `intval(...) !== intval(...)` con respaldo `-1`/`-2`. Se unificó todo a la versión estricta con respaldo numérico — en la práctica **no cambia ningún resultado real** (un `escuela_id`/`familia_id` real nunca es `0` en este esquema), pero cierra un caso teórico donde "escuela ausente" pudiera compararse como igual a "escuela ausente" en el otro lado.
- **A propósito NO se tocaron 4 sitios** que resuelven el permiso con 3 ramas OR distintas (superadmin / familia-propia / admin-cajero-de-su-escuela) en una sola variable booleana antes de negar: `cancelar_cai`, `descargar_cfdi`, `enviar_factura_correo`, y la eliminación de tarjeta guardada. Su forma no es "niega salvo que" sino "calcula si algo de 3 formas distintas es cierto" — forzarlos en los helpers de arriba habría requerido reescribir la lógica, no solo extraerla.
- **A propósito NO se tocaron los 3 sitios compuestos** ya identificados antes (`editar_cliente`, `editar_familia`, `editar_usuario`), que combinan el check de rol con una excepción de "es su propio registro" vía `&&`.
- Verificado con `php -l`, pruebas unitarias aisladas de ambos helpers nuevos (`requerir_escuela_propia`: superadmin exento, admin con escuela distinta deniega, admin con su propia escuela pasa; `requerir_familia_propia`: familia propia pasa, fila no encontrada deniega, familia distinta deniega), y una ejecución real de `api.php` por CLI. No requiere migración SQL.
- Con esto, de los ~67 checks de autorización originales en `api.php`, quedan **13 sin tocar**: 4 definiciones de helper, 4 de 3 ramas OR, 3 compuestos con `&&`, y 1 que no es de autorización (revisa si un pago ya fue confirmado por el banco). Todos documentados arriba, ninguno es un descuido.

### 5.3l `api.php` dividido: el switch de 79 casos ahora vive en `acciones/`
- `api.php` era un solo archivo de ~4,200 líneas dominado por un `switch($action)` de 79 casos. Se dividió: cada caso pasó a su propio archivo `acciones/<accion>.php` (ej. `acciones/crear_cobro.php`), y `api.php` quedó en **332 líneas** — solo config, helpers compartidos, autenticación, y un despacho que hace `require __DIR__ . '/acciones/' . $action . '.php'`.
- **Verificación antes de mover una sola línea**: se confirmó que ningún `case` dependía de fall-through (todos terminan en `break`/`exit`/`respond()`, que a su vez llama `exit`), y que no había `case` apilados ni `break`/`continue` de múltiples niveles — condición necesaria para que la división no cambiara el comportamiento.
- **Seguridad del despacho**: `$action` vem de `$_GET`, controlado por quien llama. Antes de construir la ruta del archivo se exige que cumpla `^[a-z_]+$` (los 79 nombres de acción reales lo cumplen) — sin esto, alguien podría intentar `action=../../../algo` para forzar la inclusión de un archivo arbitrario. Verificado con pruebas reales: `../config`, path traversal con `/../../`, y nombres con `;`/mayúsculas/guiones — todos rechazados antes de tocar el filesystem.
- **Verificación de equivalencia**: se comparó el `api.php` viejo (switch) contra el nuevo (despacho) llamando a las mismas acciones con los mismos parámetros — 8 acciones de solo lectura autenticadas (`listar_usuarios`, `listar_logs`, `cargar_datos`, `listar_zonas`, `listar_clabes_pool`, `superadmin_listar_referidos`, `invitaciones_listar`) dieron respuesta **byte por byte idéntica** entre ambas versiones. También se probó `login`/`invitacion_ver` (públicas) y una acción inexistente.
- ⚠️ **Estas pruebas se corrieron contra la base de datos real** (las credenciales de `config.php` son las de producción) — por eso se usaron exclusivamente acciones de solo lectura, nunca una que cree/modifique/cobre/elimine algo.
- Se agregó `acciones/.htaccess` y `lib/.htaccess` (ambos con `Require all denied`) — esos archivos PHP solo deben ejecutarse vía `require()` desde `api.php` (que ya resolvió `$pdo`/`$usuario_actual`/`$input` antes de incluirlos), nunca accedidos directo por URL.
- **Si en el futuro agregas una acción nueva**: ya no se agrega un `case` a `api.php` — se crea un archivo nuevo en `acciones/<nombre_de_la_accion>.php` con el código de esa acción (sin `case`/`break`, el archivo completo ES el cuerpo). Cualquiera que siga trabajando en este repo (incluido el bot/proceso que hace commits automáticos) necesita saber esto — si alguien vuelve a agregar un `case` directo en un switch dentro de `api.php` por costumbre, no se ejecutará nunca porque el switch ya no existe.
- No requiere migración SQL.

### 5.3m "Recordar sesión" en el login
- Antes la sesión SIEMPRE se guardaba en `sessionStorage` (se pierde al cerrar la pestaña/navegador). Se agregó una casilla "Recordar sesión en este dispositivo" en `Login.js` — si se marca, `AuthController.login()` guarda en `localStorage` en su lugar, así que la sesión sigue abierta la próxima vez que se entre, hasta que el token expire (`APP_TOKEN_TTL`, 12h en `config.php`) o se cierre sesión manualmente.
- `getSession()` ahora revisa ambos almacenamientos (localStorage primero, luego sessionStorage) y `logout()` limpia los dos, para no dejar una sesión vieja huérfana en el que no se usó.
- JS-only, no requiere migración.

### 5.3n Deduplicado: "añadir logo" aparecía dos veces en el menú de perfil
- `views/components/MenuPerfil.js` tenía dos entradas separadas: "Editar mi perfil" (con un campo para pegar el enlace de tu foto) y "Logo de la escuela" (un modal aparte, casi idéntico, para pegar el enlace del logo). Misma mecánica ("pega un enlace de imagen"), duplicada en dos pantallas.
- Se fusionó: el campo del logo de la escuela ahora vive dentro de "Editar mi perfil" (solo visible para admin/superadmin con escuela asignada). Al guardar, si el logo cambió, se manda una llamada aparte a `editar_logo_escuela` (sigue siendo una tabla distinta — `escuelas`, no `usuarios` — así que no se puede fusionar en una sola petición al backend), pero desde la vista del usuario es un solo formulario.
- Se corrigió `assets/js/app.js` (`onActualizado`) para que separe `logo_url` (pertenece a la escuela) del resto de los campos (pertenecen al usuario) al reflejar el cambio en pantalla — antes esperaba un `tipo === 'logo'` que ya no existe.
- JS-only, no requiere migración.

### 5.3o Gráfica de "Tendencia de cobranza": filtro de rango de fechas real
- La gráfica del Dashboard mostraba muy pocos días con datos. Causa real: se calculaba en el navegador a partir de `data.cobros`, que `cargar_datos` solo llena con los últimos 90 días (y con un tope de 1000 filas) — cualquier rango más largo simplemente no tenía de dónde sacar datos.
- Se creó `acciones/tendencia_cobranza.php`: agrega cobros pagados por día (`SUM`/`GROUP BY fecha`) directo en el servidor, para el rango de fechas que pida el usuario — funciona igual de rápido para 7 días que para 1 año, porque nunca manda cobro por cobro al navegador. Mismo modelo de permisos que `listar_cobros` (`requerir_escuela_propia` + familia limitada a sus propios hijos). Tope de 400 días por petición (cubre el preset más largo, 1 año, con margen) para que nadie pida un rango de décadas.
- En `Dashboard.js` se agregó un selector de rango: **1 día, 5 días, 7 días, 1 mes, 3 meses, 6 meses, 1 año**, o **rango personalizado** (dos campos de fecha). Cambiar el rango dispara la consulta al nuevo endpoint automáticamente.
- Probado contra la base de datos real (de solo lectura, sin riesgo): un rango de 30 días para la escuela #1 mostró cobros en 11 días distintos (contra los ~2 que se veían antes), confirmando que el problema era la fuente de datos, no la gráfica en sí. También se probaron rangos inválidos (hasta antes que desde) y el tope de 400 días — ambos rechazados correctamente.
- No requiere migración SQL (usa la columna `cobros.fecha` que ya existe).

### 5.3p La verdadera "gráfica de historial de cobros" (en Cobros.js) — mismo filtro de rango
- La gráfica que el usuario tenía en mente en realidad era "Tendencia del periodo" en `views/Cobros.js`, no la del Dashboard (5.3o). Tenía un filtro viejo (`filtroPeriodo`: Hoy/Semana/Mes/Año/Todo) que solo filtraba en el navegador la página ya cargada — con paginación de servidor activa eso eran cuando mucho 200 filas, así que "Año" en la práctica mostraba lo mismo que "Semana".
- Se reemplazó por el mismo selector de `RANGOS_TENDENCIA` que el Dashboard (1 día, 5 días, 7 días, 1 mes, 3 meses, 6 meses, 1 año, personalizado, más "Todo" sin filtro de fecha) y se conectó de verdad al servidor:
  - `acciones/listar_cobros.php` ahora acepta `desde`/`hasta` opcionales — la tabla y las tarjetas de resumen ("Cobrado en este listado", etc.) filtran por fecha en la base de datos, no en la página ya traída.
  - `acciones/tendencia_cobranza.php` (creado en 5.3o) ahora acepta `metodo` opcional, para que la gráfica siga respetando el filtro de método que ya tenía Cobros.js. La gráfica misma se recalculó para usar el agregado del servidor en vez de `lista`.
  - El filtro de texto de búsqueda (folio/cliente/matrícula) a propósito NO se aplica a la gráfica — no tiene un equivalente claro en una suma por día, solo afecta la tabla.
- Probado contra la base real (solo lectura): `listar_cobros` con rango de 30 días devolvió cobros dentro del rango correcto; `tendencia_cobranza` con `metodo=TC` devolvió 8 días con pagos por tarjeta en los últimos 90 días.
- **Bug encontrado y corregido de paso**: la mini-gráfica de la tarjeta "Cobrado en este listado" (arriba de la tabla) aparecía al cargar la página y luego desaparecía sola. Causa: se calculaba de `lista`, que al montar el componente refleja el caché local (varios días) pero segundos después cambia a `paginaBackend.cobros` (solo la página actual, 25 filas) en cuanto responde el servidor — con tan pocas filas, casi siempre quedaban 2 días distintos o menos y la mini-gráfica se ocultaba (su propia regla ya existente: solo se dibuja con más de 2 puntos). Se cambió para que use `tendenciaPorDia` (el mismo agregado estable que ya usa "Tendencia del periodo"), que no depende de qué página esté cargada.
- No requiere migración SQL.

### 5.3q Ficha del tutor: le faltaban campos que la BD ya tenía
- `views/components/FichaTecnica.js` (tipo `'tutor'`) solo mostraba Contacto/Correo/Teléfono/RFC/Razón social. La tabla `familias` (y `editar_familia`) ya soportan `regimen_factura`, `uso_cfdi_defecto`, `cp_factura`, `domicilio_factura` y `etiquetas` desde hace rato — el dato ya llega al frontend (`cargar_datos` hace `SELECT *`), solo no se estaba pintando.
- Se agregaron esas 5 filas a la ficha (se ocultan solas si están vacías, igual que el resto — la mayoría de familias existentes probablemente no las tengan capturadas todavía).
- JS-only, no requiere migración.

### 5.3r Frontend para el sistema de invitaciones a colegios (`registro.html`) — no existía ningún botón que llegara ahí
- El backend ya existía completo desde antes de esta sesión (llegó con el `git pull` de 5.3l): `acciones/invitacion_crear.php`, `invitacion_ver.php`, `invitacion_enviar.php`, `invitacion_resolver.php`, `invitaciones_listar.php`, más la página pública `registro.html`. Pero `grep -rln "invitacion" --include="*.js" .` no devolvía nada — ningún botón en toda la app llamaba a `invitacion_crear`, así que no había forma de generar el enlace ni de llegar a `registro.html`. Es lo que el usuario preguntó ("¿cómo ingreso a registro.html?").
- **No confundir con `distribuidor_invitar_colegio`** (el botón "+ Invitar colegio" que ya existía en `Distribuidor.js`): ese es un sistema viejo y más simple que solo inserta una fila en `distribuidor_referidos` para llevar el pipeline de comisiones — no genera token, no tiene formulario público, no crea la escuela solo. Son dos features distintas que coexisten a propósito.
- Se creó `views/components/PanelInvitaciones.js` (`PanelInvitaciones({ esSuperAdmin })`): botón "+ Generar invitación" con modal (llama a `invitacion_crear`, muestra la liga `.../registro.html?t=TOKEN` con botón de copiar — el token en claro solo se ve esta vez, el backend solo guarda el hash), más una tabla de invitaciones ya creadas (`invitaciones_listar`) con su estado (pendiente/enviado/aprobada/rechazada/expirada). Cuando `esSuperAdmin`, además aparecen botones Aprobar/Rechazar (`invitacion_resolver`) sobre las que están en estado `enviado` — resolver es superadmin-only en el backend, por eso el resto de roles no ve esos botones.
- Montado en **ambos lugares**, como pidió el usuario: `views/Escuelas.js` (superadmin, `esSuperAdmin={true}`, debajo de la grilla de escuelas) y `views/Distribuidor.js` (rol distribuidor, `esSuperAdmin={false}`, dentro de `DistColegiosView`, debajo de la tabla de "Mis colegios" — un distribuidor solo ve y genera las suyas, el backend ya filtra por `creado_por`).
- Registrado en `index.html` junto a los demás componentes (`views/components/PanelInvitaciones.js`, antes de `Escuelas.js`/`Distribuidor.js`).
- Verificado: `node --check` en el componente nuevo y en los dos archivos de vista tocados; `php -l` en los 5 archivos `acciones/invitacion_*` (ya eran de antes, pero no se habían linteado en esta sesión); prueba real de lectura contra producción (`invitaciones_listar` con un token HMAC válido de `generar_token()`) confirmando `{success:true, invitaciones:[]}` — no se generó ninguna invitación real de prueba para no dejar basura en la tabla `invitaciones_colegio`.

### 5.3s Dos bugs encontrados al probar la liga ya en producción (`registro.html` estaba roto desde que llegó por el `git pull`, no algo que rompió 5.3r)
Al generar una invitación real y abrir la liga, el usuario reportó dos fallas encadenadas — cualquiera de las dos por sí sola habría dejado la página inservible:
1. **404 por mayúscula/minúscula**: el archivo estaba trackeado como `Registro.html` (R mayúscula), pero `invitacion_crear.php` arma la liga con `registro.html` (minúscula). En Windows nadie lo notó porque el filesystem no distingue mayúsculas; el servidor de producción es Linux y sí distingue, así que la liga generada apuntaba a un archivo que "no existía" — devolvía el 404 genérico del hosting (de ahí los errores de jQuery/`simple-expand.min.js` en consola: son del template de error del hosting, no de esta app). Arreglado con `git mv` (rename de solo-mayúscula, requiere el truco de doble `git mv` vía nombre temporal en Windows porque el filesystem no deja tener ambos nombres a la vez para que git detecte el cambio).
2. **CSP bloqueaba el único `<script>` de la página**: `registro.html` traía toda su lógica (verificar el token, pintar el formulario, enviarlo) en un `<script>` inline. El `.htaccess` de este proyecto ya endurece `script-src` a `'self' https://unpkg.com https://cdnjs.cloudflare.com` **sin** `'unsafe-inline'` — a propósito, según su propio comentario ("el único script inline que había, la animación de bienvenida, se movió a `assets/js/intro-splash.js` para que esto pudiera ser estricto de verdad"). `registro.html` llegó después de ese endurecimiento y nadie lo migró, así que quedaba 100% muerto: ni siquiera el "Verificando tu liga…" avanzaba, porque NINGÚN JS de la página podía ejecutarse. Arreglado igual que la animación: se sacó el bloque a `assets/js/registro.js` (mismo contenido, sin cambios de lógica) y `registro.html` ahora solo tiene `<script src="assets/js/registro.js?v=1787360000"></script>`.
- Si el mecanismo de despliegue de este proyecto NO borra archivos renombrados/eliminados del lado del servidor (algunos deploys por FTP/rsync sin `--delete` no lo hacen), podría quedar un `Registro.html` huérfano en el servidor con el contenido viejo (inline, roto por CSP) — inofensivo porque nada en el código genera ligas con esa mayúscula, pero si alguien lo encuentra a mano seguirá sin funcionar. No es necesario limpiarlo con urgencia, solo tenerlo presente.
- Verificado: `node --check` en `assets/js/registro.js`; revisión visual de que `registro.html` quedó con el mismo `id`s (`cuerpo`, `app`) que el script espera.

### 5.3t Cierre de huecos reales en el flujo de invitaciones (encontrados al preguntar "¿debería el SA ver los links por distribuidor?")
La pregunta del usuario destapó algo más importante que la parte de UI: **un distribuidor que genera su propia invitación (5.3r) nunca recibía comisión si se aprobaba**. `distribuidor_comisiones.php` calcula comisiones haciendo JOIN de `cobros` contra `distribuidor_referidos` — esa tabla es la única fuente de verdad de "qué colegio le pertenece a qué distribuidor". El flujo viejo (`distribuidor_invitar_colegio`) sí inserta ahí; `invitacion_resolver.php` (5.3r, llegó con el `git pull`) solo insertaba en `escuelas`, nunca en `distribuidor_referidos` — un distribuidor podía traer un colegio entero y quedarse sin comisión sin que nadie lo notara hasta el fin de mes.
1. **`acciones/invitacion_resolver.php`**: al aprobar, si `invitaciones_colegio.distribuidor_id` no es null, ahora también inserta en `distribuidor_referidos` (mismo `escuela_id` recién creado, `estado='activo'` porque el colegio ya queda activo de inmediato, `comision_pct=5.00` — el mismo default que usa `distribuidor_invitar_colegio.php`; el superadmin puede ajustarlo después desde `superadmin_editar_referido`).
2. **Número de alumnos**: el formulario de `registro.html` no lo pedía, aunque el flujo viejo sí lo captura (`distribuidor_invitar_colegio.num_alumnos`) y ahora también se necesita para poblar `distribuidor_referidos.num_alumnos`. Se agregó el campo (opcional) a `registro.html`/`registro.js`, y `acciones/invitacion_enviar.php` ahora lo guarda dentro de `datos_enviados`.
3. **Visibilidad por distribuidor para el SA**: `acciones/invitaciones_listar.php` ahora hace `LEFT JOIN usuarios` (solo en la rama superadmin) y devuelve `creado_por_nombre`/`creado_por_rol`. `PanelInvitaciones.js` muestra esa columna ("Generado por") solo cuando `esSuperAdmin`, más un buscador simple (filtra por distribuidor o contacto) — no hace falta una pantalla aparte por distribuidor, con esto el SA ya puede ver/filtrar todo desde el mismo panel. Esto responde la pregunta: **no hacía falta un segundo lugar para que el SA genere invitaciones** (ya podía, `invitacion_crear` siempre aceptó `superadmin`) — lo que faltaba era poder distinguir de quién era cada una.
4. **"No me dice nada, solo aparece aprobar o no"**: las invitaciones en estado `enviado` sí traen los datos que el colegio llenó (`datos_enviados` ya venía en la respuesta de `invitaciones_listar` desde 5.3r, pero el frontend los descartaba). Se reemplazaron los botones sueltos de Aprobar/Rechazar por un botón "Revisar"/"Ver" que abre `ModalDetalleInvitacion`: muestra colegio, correo, teléfono, núm. de alumnos, RFC, RVOE, dirección y quién generó la liga — y solo ahí, ya con los datos a la vista, aparecen Aprobar/Rechazar (solo si `esSuperAdmin` y `estado==='enviado'`, igual que antes).
5. **Rendimiento de `registro.html`**: cargaba `assets/css/main.css` completo (53 KB, pensado para toda la SPA) solo por 4 variables CSS. Se quitó ese `<link>` y se dejaron los valores literales en el `<style>` propio de la página (y se restauró a mano el reset `*{box-sizing:border-box;margin:0;padding:0}` que antes venía gratis desde `main.css`, para no romper el layout). Un request y ~53 KB menos en una página pública de un solo formulario.
- No tocado a propósito: qué tan "completo" debe ser el formulario de auto-registro. Los demás campos de `escuelas` (`clave`, `plan`, `color`, `permite_planteles`, `clabe_fija`) son decisiones administrativas que el superadmin ya define al aprobar (`crear_escuela` los pide todos; `invitacion_resolver` los deja en default: `plan='basico'`) — pedírselos al colegio en el formulario público no tendría sentido, por eso el único campo que se agregó fue `num_alumnos`, que sí es un dato que el colegio conoce y que además ya hacía falta para comisión.
- Verificado: `php -l` en los 3 archivos PHP tocados; `node --check` en `PanelInvitaciones.js` y `registro.js`; prueba real de lectura contra producción (`invitaciones_listar` como superadmin) confirmando que `creado_por_nombre`/`creado_por_rol` llegan bien (de paso, esa prueba mostró la invitación real #1 que el usuario ya había probado de punta a punta — colegio "FESC 4", en estado `rechazada` — evidencia directa de que antes solo veía Aprobar/Rechazar sin poder revisar los datos). No se probó `invitacion_resolver` con una aprobación real (crearía una escuela real en producción) — la lógica se verificó por lectura cuidadosa contra el patrón ya usado en `distribuidor_invitar_colegio.php`/`superadmin_editar_referido.php`.
- Nota aparte, sin relación con el código: durante esta verificación `test.grupoideasmx.com` estuvo momentáneamente inalcanzable (503 con la página genérica del hosting, luego timeout total del dominio) — se resolvió solo en un par de minutos. Es un problema del hosting compartido, no algo causado por estos cambios (un error de sintaxis PHP se habría visto como 500 de la propia app, no como timeout de todo el dominio).

### 5.3u Al aprobar una invitación, el colegio quedaba activo pero sin nadie que pudiera entrar
El usuario probó el flujo de punta a punta y preguntó por qué, al aprobar, no le llegaba un correo con sus credenciales. Investigando: `invitacion_resolver.php` (5.3r) traía un comentario diciendo "el usuario administrador se crea aparte, con `crear_usuario`" — es decir, aprobar una invitación dejaba el colegio activo en la BD pero **sin ningún usuario que pudiera iniciar sesión**, y sin avisar a nadie que faltaba ese paso manual. Revisé el resto del código por si el envío de credenciales ya existía en algún otro flujo parecido (`crear_plantel.php`, que también crea automáticamente el admin de un plantel) — no: ahí tampoco se manda correo, la contraseña generada simplemente se devuelve en la respuesta para que el superadmin la copie y la pase a mano. **En ningún lugar de la aplicación se han enviado credenciales por correo automáticamente hasta ahora** — no es una regresión de esta sesión, es un vacío del proyecto que la propia feature de auto-registro (pensada para que nadie tenga que hacer nada a mano) dejó en evidencia.
- `acciones/invitacion_resolver.php`, al aprobar: ahora también crea la cuenta admin (`rol='admin'`, contraseña aleatoria de `random_bytes(8)`) usando el correo institucional que el colegio mandó en el formulario, y le envía un correo de bienvenida con usuario/contraseña vía `enviar_correo()` (la misma función que ya usan `enviar_factura_correo.php` y `cron_recordatorios.php` — nada nuevo en la infraestructura de correo).
- Casos raros cubiertos sin romper la aprobación (crear el colegio siempre es lo prioritario, la cuenta de acceso es secundaria):
  - Si el correo institucional ya tiene una cuenta en `usuarios`, no se duplica ni se sobreescribe — se aprueba igual y la respuesta trae `usuario_creado:false` para que el superadmin lo resuelva a mano desde Usuarios si hace falta.
  - Si `enviar_correo()` falla (SMTP caído, etc.), la cuenta ya quedó creada — no se pierde el trabajo. La respuesta trae `correo_enviado:false` y la contraseña en claro (única vez que se devuelve, mismo criterio que el token de invitación) para que el superadmin la transmita él mismo.
- `PanelInvitaciones.js`: al aprobar, ahora se le informa al superadmin qué pasó — mensaje de éxito si se mandó el correo, o un modal con usuario/contraseña (con botón de copiar) si el correo falló y hay que relayarla a mano.
- Verificado: `php -l`; revisión cruzada de la estructura del INSERT contra `crear_plantel.php` (que inserta usuarios con el mismo subconjunto de columnas, confirmando que son opcionales/nulas); `node --check`. No se probó una aprobación real (crearía una escuela y una cuenta real en producción, y mandaría un correo real).

### 5.3v Aprobar una invitación tronaba con "Duplicate entry '' for key 'clave'" — y ya había dejado una escuela real coja
El usuario probó una aprobación real y `invitacion_resolver.php` truena con `SQLSTATE[23000]... Duplicate entry '' for key 'clave'`. `escuelas.clave` es única (`crear_escuela.php`/`editar_escuela.php` siempre la piden y la validan antes de insertar/editar), pero el `INSERT INTO escuelas` de `invitacion_resolver.php` **nunca la incluía** — no es un bug de hoy, ya estaba así desde que este archivo llegó por el `git pull` (5.3r); tiene sentido que nadie lo hubiera visto porque nunca se había hecho una aprobación real hasta ahora. Al faltar la columna, MySQL usaba su default (cadena vacía), así que la PRIMERA aprobación exitosa se quedó con `clave=''` sin problema — y CUALQUIER aprobación después de esa chocaba contra esa misma fila por el índice único.
- **Daño real ya hecho, encontrado leyendo la BD (solo lectura)**: la escuela **#10** ("Facultad de Estudios Superiores Cuautitlán UNAM Campus 4", de la invitación #2) ya se había creado con `clave=''`, y además — como esa aprobación pasó *antes* de que se agregara la creación automática de usuario (5.3u) — **0 filas en `usuarios` para `escuela_id=10`: nadie podía iniciar sesión ahí**, aunque la escuela apareciera como activa.
- **Fix de código**: `invitacion_resolver.php` ahora genera la `clave` a partir del nombre del colegio + el id de la invitación (ej. `FACULT-2`) — el id de invitación nunca se repite, así que nunca choca con otra escuela, sin necesitar un query extra de verificación ni un loop de reintento. El formulario público nunca pide `clave` a propósito (ver nota de 5.3t/5.3u: es una decisión administrativa, no algo que el colegio deba inventar).
- **Reparación de la escuela #10**: se le preguntó al usuario cómo quería resolver el registro ya roto (arreglar todo automáticamente vs. solo la clave vs. hacerlo él mismo desde la UI). Eligió **"solo la clave, sin crear usuario ni mandar correo"** — se le asignó `clave='FACULT-2'` con un script de una sola vez (mismo algoritmo que ahora usa el código), verificando antes que no chocara con ninguna otra escuela y confirmando después con una lectura que no queda ninguna escuela con `clave` vacía. La cuenta de acceso para esa escuela sigue pendiente — queda a criterio del usuario crearla luego con "Crear usuario" en Usuarios.js cuando quiera darle acceso a ese colegio.
- Verificado: `php -l`; se corrigió/confirmó por lectura directa a la BD de producción (no se volvió a intentar una aprobación real después del fix, por no crear otra escuela de prueba).

### 5.3w `views/Alumnos.js` estaba duplicado de punta a punta — 1300 líneas muertas desde hace quién sabe cuánto
El usuario reportó "falta paginación en estudiantes". Al abrir el archivo: `function Alumnos({` aparecía DOS VECES (línea 9 y línea 1312 de un archivo de 2607 líneas), con las dos mitades byte-idénticas salvo el bloque `_jsxDEV`/comentario de cabecera que quedó pegado en medio — un accidente de concatenación (dos copias del mismo archivo pegadas una tras otra), no dos versiones distintas a propósito. En JS sin modo estricto esto NO truena: la ÚLTIMA declaración de `function Alumnos` gana silenciosamente y la primera mitad (1300 líneas) queda como código muerto — mismo tipo de riesgo que la colisión `usePaginacion`/`Paginador.js` de 5.3h, aquí ya consumado dentro de un solo archivo.
- La mitad viva (la segunda) **ya tenía paginación real** del lado del servidor (`cargar_datos` con `pagina_clientes`/`por_pagina_clientes`, footer "Página X de Y" con Anterior/Siguiente) — no había que construirla. El archivo se dejó en 1304 líneas, con esa única copia.
- Como de todos modos se reescribieron los bytes del archivo, se le subió la versión de cache-busting en `index.html` (`views/Alumnos.js?v=1787360000` → `?v=1787370000`) — si no, el navegador podría seguir sirviendo desde caché la versión vieja (duplicada) indefinidamente, sin importar el fix.
- Si después de refrescar el usuario sigue sin ver paginación, hace falta confirmarlo en vivo — no se pudo probar en un navegador real desde aquí.
- Verificado: `node --check` sobre el archivo final; se confirmó por `diff` que las dos mitades eran idénticas antes de descartar la primera (cero riesgo de perder lógica real).

### 5.3x Cómo deshacer una importación CSV de prueba
El usuario probó `importar_alumnos` con 500 filas sintéticas y preguntó cómo borrar todo. No existe (ni se construyó) un "deshacer importación" — pero no hace falta: la escuela usada para la prueba (**#12, "Facultad de Estudios Superiores Cuautitlán UNAM Campus 4", clave `FACULT-3`**) resultó tener EXACTAMENTE 400 clientes activos, 319 familias y 320 usuarios — el 100% de lo que dejó esa importación (319 tutores + 1 admin de la propia aprobación de invitación, ver 5.3u), nada más. `eliminar_escuela.php` (ya existente, usado desde Escuelas.js) borra en cascada dentro de una transacción: `cobro_items → pagos_recurrentes_generados → recordatorios → cobros → clabe_pool → clientes → familias → productos → usuarios → planteles → escuelas`, y además las FK de `familias`/`usuarios`/`clientes` hacia `escuelas.id` ya son `ON DELETE CASCADE` (confirmado por lectura directa a la BD) — así que borrar la escuela #12 desde la UI (con el flujo de doble confirmación que ya trae: primero avisa cuántos registros hay, luego pide escribir la clave exacta para forzar) limpia el 100% de la prueba en una sola acción, sin necesitar ningún script nuevo.
- No se ejecutó el borrado — es una acción irreversible sobre 320 cuentas reales y se dejó a que el usuario la confirme él mismo desde la UI, tal como esa pantalla ya está diseñada para exigir.

### 5.3y Eliminar escuela tronaba si un distribuidor la había referido
El usuario intentó borrar la escuela #12 (la de la prueba de CSV, 5.3x) y `eliminar_escuela.php` truena con `SQLSTATE[23000]... Cannot delete... CONSTRAINT fk_referido_escuela FOREIGN KEY (escuela_id) REFERENCES escuelas (id)`. Causa directa: `eliminar_escuela.php` borra en cascada `cobro_items/pagos_recurrentes_generados/recordatorios/cobros/clabe_pool/clientes/familias/productos/usuarios/planteles` antes de borrar la escuela — pero nunca borraba `distribuidor_referidos`, y a diferencia de `familias`/`usuarios`/`clientes`, esa tabla NO tiene `ON DELETE CASCADE` (confirmado por lectura directa a la BD). Antes de esta sesión esa fila casi nunca existía salvo que un superadmin la vinculara a mano (`superadmin_editar_referido`); desde 5.3t, `invitacion_resolver.php` la crea automáticamente al aprobar una invitación de un distribuidor — la escuela #12 es precisamente ese caso (viene de una invitación de distribuidor), así que el bug pasó de "casi nunca" a "cada vez que se intente borrar un colegio traído por un distribuidor".
- Se agregó `DELETE FROM distribuidor_referidos WHERE escuela_id IN (...)` a la cascada (antes de `usuarios`, sin que el orden importe realmente). También se sumó a la cuenta de "esto ya tiene datos, ¿seguro?" (`$hayDatos`) y al resumen final — antes, un colegio con una sola fila de comisión de distribuidor y CERO alumnos/cobros/planteles/CLABEs se hubiera borrado SIN pedir confirmación forzada, perdiendo silenciosamente ese historial de comisión.
- No se re-intentó el borrado de la escuela #12 — queda para que el usuario lo repita desde la UI ahora que el bug de fondo está resuelto.
- Verificado: `php -l`.

### 5.3z Caja sin tope al elegir alumno + Usuarios sin paginación ni límite de "Super Admin" visible
Dos vistas más se quedaron sin protección al crecer una escuela a cientos de registros (mismo patrón que 5.3w, encontrado por el usuario probando con la escuela de 400 alumnos):
- **`views/Caja.js`**: el selector de cliente (buscar a quién cobrarle) pintaba `data.clientes` completo sin buscar nada — con 400 alumnos activos, abrir el buscador mostraba los 400 de un jalón. Se le puso un tope de 40 (mismo criterio que el buscador de familia en `Alumnos.js`), con aviso "Mostrando 40 de N — escribe para acotar" cuando se corta.
- **`views/Usuarios.js`**: la tabla de usuarios no tenía ningún tope — con la importación CSV creando una cuenta de tutor por familia (319 en la prueba de 5.3x), la tabla se iba a cientos de filas sin paginar. Se conectó el hook ya existente `usePaginacion`/`<Paginador>` (de `views/components/Paginador.js`, ya usado en `Familias.js`) sobre la lista ya filtrada por rol/búsqueda.
- **Además, a pedido del usuario**: un admin (no superadmin) nunca puede tener ni ver cuentas `superadmin` — `listar_usuarios.php` ya las excluye del lado del servidor, y el selector de rol al crear/editar usuario (`rolesQuePuedeCriar`) ya solo ofrece `cajero`/`familia` a un admin. Pero la tarjeta de estadísticas "Super Admin" y el filtro-chip del mismo nombre se mostraban siempre, con conteo en 0 para un admin — puro ruido de UI que además insinúa un rol que un admin no puede usar. Se ocultan ambos cuando el que mira no es superadmin.
- Verificado: `node --check` en los dos archivos; se subió la versión de cache-busting de ambos en `index.html` (misma razón que 5.3w: el navegador podría seguir sirviendo la versión vieja en caché).

### 5.3aa "No me llega el correo" al aprobar una escuela — se agregó reenvío, no se pudo confirmar la causa raíz al 100%
El usuario reportó que el correo de bienvenida (5.3u) nunca le llegó tras aprobar una invitación. Diagnóstico:
- Se probó `enviar_correo()` directo contra las credenciales SMTP reales de producción (`mail.pagalaescuela.com`) desde esta sesión — **funcionó** (`{"success":true}`), así que el `SMTP_PASS` de `config.php` sí es el real pese al comentario "&lt;-- Reemplazar..." (comentario obsoleto, no un problema activo).
- No se pudo probar el envío ejecutándose DESDE el propio servidor de producción (`test.grupoideasmx.com`) — solo hay acceso a su base de datos y a su API por HTTP, no a ejecución de código en ese servidor. La hipótesis más probable es que el hosting compartido bloquee conexiones SMTP salientes desde PHP aunque las credenciales sean válidas (común en hosting compartido tipo Hostinger) — pero queda sin confirmar.
- Se creó `acciones/reenviar_credenciales.php` (superadmin, o admin sobre cajero/familia de su propia escuela — mismo criterio que `puedeEditar` en `Usuarios.js`): genera una contraseña nueva (la anterior nunca se puede "reenviar", solo se guarda su hash) y reintenta el correo. Si vuelve a fallar, la respuesta trae la contraseña en claro para que se transmita a mano (mismo criterio que el token de invitación y la aprobación de escuela).
- Botón "Reenviar" nuevo en cada fila de `views/Usuarios.js` (icono `history`, junto a "cerrar sesiones"), con el mismo modal de contraseña-de-respaldo ya construido en `PanelInvitaciones.js` (se reutiliza tal cual — `PanelInvitaciones.js` carga antes en `index.html`, así que `ModalCredencialesTemp` ya está disponible globalmente).
- Verificado en vivo: se reprodujo el flujo contra la cuenta real del reporte (usuario #340, `victorhome2023@outlook.com`, escuela #13) — el botón ya está listo para usarse en cuanto el usuario quiera probarlo; el resultado (si el correo por fin sale o no) es la prueba definitiva que hacía falta.

### 5.3ab `editar_usuario` tronaba con 500 al dejar el correo igual al de otra cuenta
Mientras probaba lo de arriba, el usuario intentó editar el correo de la cuenta #340 y le salió `500` en `editar_usuario`. Causa: `usuarios.email` es `UNIQUE`, pero `editar_usuario.php` nunca revisaba eso antes de hacer el `UPDATE` — dependía de que MySQL rechazara el duplicado, y el único `catch` que tenía el código estaba pensado SOLO para el caso de que faltara la columna `zona_id` (migración vieja); cualquier OTRO error de SQL, incluida esta violación de unicidad, se relanzaba tal cual (`throw $e;`) y terminaba como 500 sin mensaje. Reproducido en vivo a propósito (intentando poner en la cuenta #340 el correo de la #1) para confirmar antes de tocar nada — mismo 500 exacto.
- Se agregó el mismo chequeo que ya tiene `crear_usuario.php`/`editar_escuela.php` (`SELECT id FROM usuarios WHERE email = ? AND id != ?`) antes de armar el `UPDATE`, respondiendo `'Ese correo ya está registrado por otra cuenta.'` en vez de tronar.
- Además, el `catch` que antes hacía `throw $e` para cualquier error no relacionado con `zona_id` ahora responde con el mensaje del error en vez de dejarlo sin capturar — cierra la puerta a que cualquier otra violación de constraint no anticipada también termine en 500.
- Verificado: `php -l`; reproducción en vivo del bug ANTES del fix (confirmó el 500 exacto reportado) — el fix ya está listo, pendiente del próximo auto-sync para volver a probarlo en vivo.

### 5.4 Columnas de la base de datos — pendientes documentados (no tocar sin leer esto)
- **`usuarios.zona` / `planteles.zona` (texto) vs `zona_id` (FK a la tabla `zonas`)**: es una migración a normalizado que ya está en curso desde antes, NO un descuido. Hoy solo las filas nuevas (distribuidores #11/#12) tienen `zona_id` poblado — el resto de usuarios/planteles viejos sigue con `zona_id = NULL` y solo el texto libre. **No borres las columnas `zona` (texto) todavía** — primero hay que backfillear `zona_id` en todas las filas viejas cruzando contra `zonas.nombre`, confirmar que quedó 100% poblado, y solo entonces dropear el texto.
- **`escuelas.clabe_fija`**: legado, reemplazado por el sistema de `clabe_pool` (CLABEs individuales). Confirmado que ningún archivo PHP la lee ya (ni siquiera los webhooks de SPEI/CLABE) — es segura de eliminar cuando quieras, no es urgente.

### 5.3ac Se dejó de mandar contraseñas por correo — Outlook las marcaba como phishing
Cerrando el hilo de 5.3aa: el correo SÍ se estaba enviando (confirmado en vivo, `correo_enviado:true` desde el propio servidor de producción) y SÍ llegaba — pero a la carpeta de spam de Outlook/Hotmail. Se investigó el DNS de `pagalaescuela.com`: SPF y DKIM ya estaban bien configurados, pero **faltaba el registro DMARC** (`_dmarc.pagalaescuela.com`) — el usuario lo agregó (`v=DMARC1; p=none; rua=mailto:contacto@pagalaescuela.com`, verificado propagado con Google DNS y Cloudflare DNS). Con eso, el correo pasó de "no llega" a "llega, pero a spam" — mejora real, pero no resuelve el fondo.
El motivo de fondo: **el correo mandaba la contraseña en texto plano** ("Usuario: X, Contraseña temporal: Y") — un patrón de contenido casi idéntico al de un correo de phishing típico, que los filtros de Microsoft penalizan mucho más agresivamente que Gmail, sobre todo viniendo de un dominio con poco historial de envíos hacia ellos. Se decidió no perseguir más ajustes de reputación de dominio y en vez de eso quitar la causa de raíz: **ya no se manda ninguna contraseña por correo, en ningún flujo**.
- **Nuevas columnas** en `usuarios` (migración manual, ya corrida por el usuario vía phpMyAdmin): `activacion_token_hash CHAR(64)`, `activacion_expira DATETIME` — mismo patrón que `invitaciones_colegio.token_hash`: solo se guarda el hash del token, nunca el token en claro.
- **Dos acciones públicas nuevas** (agregadas a `$acciones_publicas` en `api.php`): `acciones/activar_cuenta_ver.php` (valida el token, devuelve nombre/correo) y `acciones/activar_cuenta_confirmar.php` (valida el token, guarda la contraseña que el propio usuario eligió, invalida el token de un solo uso y fuerza `sesion_valida_desde = NOW()`).
- **Página pública nueva** `activar_cuenta.html` + `assets/js/activar_cuenta.js` — mismo patrón ya corregido de `registro.html`/`registro.js` desde el inicio (sin `<script>` inline por el CSP del `.htaccess`, sin cargar `main.css` completo).
- **`acciones/invitacion_resolver.php`** (aprobar escuela): en vez de generar una contraseña y mandarla, genera un token de activación (`bin2hex(random_bytes(32))`), inserta al usuario con una contraseña real ALEATORIA que nadie conoce (placeholder solo porque `password_hash` es `NOT NULL`) y manda por correo un enlace a `activar_cuenta.html?t=TOKEN` en vez de "usuario y contraseña". Si el correo falla, la respuesta trae el enlace (no una contraseña) para que el superadmin lo comparta a mano.
- **`acciones/reenviar_credenciales.php`**: mismo cambio — ya no resetea la contraseña de inmediato (eso desloguearía al usuario aunque todavía no haya visto el correo nuevo); genera y guarda un token de activación nuevo, y la contraseña actual sigue funcionando hasta que la persona de verdad entre al enlace y ponga una nueva.
- **Frontend**: se generalizó el modal `ModalLigaRegenerada` de `PanelInvitaciones.js` (ahora `ModalLigaCopiar`, con `titulo`/`mensaje` configurables) para cubrir los tres casos que antes tenían componentes separados o mostraban una contraseña (`ModalCredencialesTemp`, eliminado por completo — ya no aplica). Se actualizó el texto en `PanelInvitaciones.js` (aprobar escuela) y `Usuarios.js` (botón "Reenviar") para hablar de "enlace de activación" en vez de "contraseña".
- Verificado: `php -l`/`node --check` en todos los archivos tocados; probado en vivo contra producción después de correr la migración — `reenviar_credenciales` sobre la cuenta real del caso (usuario #340, `victorhome2023@outlook.com`) generó el enlace y lo mandó (`correo_enviado:true`). Falta la confirmación final del usuario de si este correo (sin contraseña, solo un enlace de activación) ya no cae en spam.

### 5.3ad Bug real: `dataSegura`/`mergeScoped` en `app.js` no incluían los campos nuevos de paginación de alumnos
Este es el que causó horas de diagnóstico de "solo aparecen 25 alumnos" en escuelas grandes recién subidas por CSV (400+ alumnos), a pesar de que la API, `cargar_datos.php` y `Alumnos.js` ya estaban correctos por separado (verificado con `curl` directo contra producción). La causa real: `app.js` arma `dataScopeed` (lo que de verdad reciben las vistas) a partir de `dataSegura`, una lista blanca de campos escrita ANTES de que existieran `clientes_total`/`clientes_activos_total`/`clientes_pagina`/`clientes_por_pagina` — nunca se agregaron ahí, así que las vistas siempre los veían `undefined` y caían al conteo de solo la página cargada (25), sin importar que el backend regresara el total real. Lo mismo pasaba en sentido inverso en `mergeScoped()`: una actualización escopada (ej. la paginación de `Alumnos.js`) nunca lograba propagar esos 4 campos de vuelta al estado global. Ni la caché del navegador ni ningún bug de lógica de paginación tenían que ver — se descartó cache-busting, `Cache-Control` en `.htaccess`/`api.php`, y `cache:'no-store'` en los `fetch()` antes de encontrar esto (esos cambios se dejaron igual, son buena práctica aunque no eran la causa).
- Se agregaron los 4 campos a `dataSegura` (paso a través de `data.clientes_total`, etc.) y a `mergeScoped()` (prefiriendo el valor de la actualización escopada, cayendo al global si no viene).
- Lección para la próxima vez que se agregue un campo nuevo a la respuesta de `cargar_datos.php`: también hay que agregarlo a `dataSegura`/`mergeScoped` en `app.js`, o nunca le va a llegar a ninguna vista aunque la API y el estado de nivel superior sí lo tengan.

### 5.3ae Bug real (encontrado de paso): "Editar mi perfil" tronaba con "id requerido" al guardar el logo de la escuela
`MenuPerfil` recibía `escuela: escuelaActiva` desde `app.js` — pero `escuelaActiva` es el ID numérico de la escuela, no el objeto. `MenuPerfil.js` esperaba `escuela.id`/`escuela.logo_url`, así que siempre mandaba `id: undefined` a `editar_logo_escuela.php`. Como la foto de perfil y el logo se guardan en la misma llamada, el error del logo tapaba que la foto sí se había guardado — por eso "no se veía ningún cambio". Corregido: se pasa la variable `escuela` (el objeto ya resuelto que existe en el mismo scope) en vez de `escuelaActiva`, y se corrigió el mismo error en el callback `onActualizado` (comparaba `e.id === escuelaActiva.id` en vez de `e.id === escuelaActiva`).

### 5.3af CSV de alumnos: cuentas de tutores migradas a enlaces de activación + botón para asignar CLABE a todos
Dos pendientes del usuario tras subir una escuela de 500+ alumnos por CSV:
- **Contraseñas en texto plano → enlaces de activación**: `importar_alumnos.php` generaba una contraseña de 8 caracteres por tutor nuevo y la mostraba en una tabla para copiar a mano — mismo patrón de "contraseña en el cuerpo del correo" que 5.3ac ya había quitado en otros flujos. Ahora genera el mismo token de activación de un solo uso (72h de expiración, igual que activación de cuenta normal) y **manda el correo automáticamente** a cada tutor nuevo. Con CSV de cientos de filas, mandar un correo síncrono por cada uno arriesgaba timeout del servidor — se le puso un presupuesto de 40 segundos (medido desde `REQUEST_TIME_FLOAT`, así que ya cuenta el tiempo que tomó procesar el CSV completo): los correos que no alcancen a mandarse dentro de ese presupuesto quedan con su enlace visible en la tabla de resultados de `Alumnos.js` (con botón "Copiar enlace") para compartirlos a mano, igual que ya hacía `invitacion_resolver.php` cuando el correo automático fallaba.
- **Botón "Generar CLABE a todos"**: nueva acción `acciones/asignar_clabe_pool_masivo.php` — misma fuente y reglas que `asignar_clabe_pool.php` (pool `libre`/`liberada` de la escuela, una CLABE por alumno) pero en una sola transacción sobre TODOS los alumnos activos sin CLABE, para no tener que darle clic uno por uno en escuelas grandes. Si el pool se agota a medio camino, reporta cuántos quedaron sin asignar (para que se importen más CLABEs) en vez de tronar.

### 5.3ag "¿No recuerdas tu contraseña?" en el login (recuperación self-service)
Reutiliza exactamente el mismo mecanismo de token que activación de cuenta (`activacion_token_hash`/`activacion_expira` en `usuarios`, y las mismas páginas/acciones `activar_cuenta.html`, `activar_cuenta_ver.php`, `activar_cuenta_confirmar.php`) — fijar una contraseña nueva es la misma operación sin importar si es la primera vez o un olvido, así que no hizo falta ninguna migración ni endpoint de "confirmar" nuevo.
- **Nueva acción pública** `acciones/recuperar_password_solicitar.php`: recibe un correo, y si existe una cuenta activa con ese correo le genera un token con **10 minutos de expiración** (no 72h — a diferencia de una invitación por correo que puede tardar días en revisarse, aquí ya hay una cuenta funcionando, así que la ventana se acorta a propósito) y le manda el enlace por correo. La respuesta es **siempre la misma exista o no la cuenta** (`"Si el correo está registrado, te enviamos un enlace..."`) para no poder usar este formulario como forma de enumerar qué correos están dados de alta.
- El enlace apunta a `activar_cuenta.html?modo=reset&t=TOKEN` — mismo backend, pero `assets/js/activar_cuenta.js` ahora lee `modo` de la URL y cambia el texto en pantalla ("Restablecer contraseña" / "Guardar nueva contraseña" en vez de "Activar cuenta" / "Activar mi cuenta") para que no sea confuso para alguien cuya cuenta ya estaba activa.
- **`views/Login.js`**: nuevo link "¿No recuerdas tu contraseña?" bajo el campo de contraseña, que cambia el formulario a un modo de recuperación (solo pide correo) sin salir de la pantalla de login. `AuthController.js` gana `recuperarPassword(email)`.
- Pendiente (no se construyó en esta pasada, decisión explícita del usuario): un rol "soporte" para que alguien distinto de superadmin/admin pueda dar seguimiento a distribuidores/quejas/incidencias sin tener acceso total — queda documentado aquí como próximo paso, con la advertencia de definir bien el límite de permisos antes de tocar código.

### 5.3ah Bug real: el selector de familias en Usuarios.js/"Mi equipo" leía `data.clientes` en vez de `data.familias`
Reportado como "no aparecen familias al crear la cuenta familiar" y, probablemente la misma causa, "¿no son demasiados pasos tener que crear la familia y luego la cuenta?" — lo segundo probablemente era una consecuencia de lo primero: la familia normalmente YA existe (se crea sola al importar el CSV de alumnos o al dar de alta un alumno con correo de tutor), pero como el selector nunca la mostraba, parecía que había que crearla de cero.
- Causa: `familiasUnicas` en `Usuarios.js` se armaba recorriendo `data.clientes` buscando `familia_id` — pero `data.clientes` solo trae LA PÁGINA actual de alumnos (25 por defecto, ver 5.3ad), así que en cualquier escuela con más de una página casi ninguna familia real llegaba a aparecer. `data.familias` ya viene completa sin paginar (hasta 1000 por escuela, ver `cargar_datos.php`) y con su propio campo `nombre` — se cambió `familiasUnicas` para leer de ahí directamente.
- De paso, `acciones/crear_usuario.php` (creación manual de admin/cajero/familia/superadmin/distribuidor desde Usuarios.js) era el último flujo que le seguía pidiendo al operador inventar y escribir una contraseña a mano — inconsistente con el resto del sistema desde 5.3ac/5.3af. Se migró al mismo patrón: ya no pide contraseña, genera un enlace de activación (72h) y lo manda por correo; si el correo falla, `Usuarios.js` muestra el enlace con `ModalLigaCopiar` (mismo componente que ya usaba `reenviarCredenciales`).

### 5.3ai Crear familia y su cuenta en un solo paso, desde cualquiera de las dos pantallas
Seguimiento a 5.3ah: aunque las familias ya aparecían bien en el selector, seguía habiendo dos viajes de pantalla para dar de alta a un tutor sin alumnos todavía. Se resolvió en ambos sentidos, cada uno opcional (igual que ya hace la importación de CSV: solo crea la cuenta si hay correo):
- **`views/Familias.js`** (crear familia): nueva casilla "Darle acceso al portal a este correo" — si se marca (y hay correo), justo después de crear la familia se crea también la cuenta `familia` vinculada, vía `acciones/crear_usuario.php` (enlace de activación por correo, igual que el resto del sistema). Si el correo falla, aparece `ModalLigaCopiar` con el enlace para compartir a mano. Si no se marca, el comportamiento es exactamente el de antes.
- **`views/Usuarios.js`/"Mi equipo"** (crear cuenta `familia`): botón "+ Nueva familia" junto al selector de familia — abre un mini-formulario (nombre + correo opcional) que llama a `crear_familia` y selecciona la familia recién creada de una vez, sin cerrar el modal ni perder lo demás capturado. Detalle técnico: `Usuarios.js` no recibe `setData` (a diferencia de `Familias.js`/`Alumnos.js`), así que la familia creada así se guarda en un estado local propio (`familiasNuevas`) que se fusiona con `data.familias` solo para esta sesión — al recargar la página ya viene en `data.familias` como cualquier otra, no se pierde nada.

### 5.3aj Export CSV de comisiones de distribuidores (no existía) + helper compartido `CSVExport.js`
No existía NINGUNA exportación de comisiones en el sistema (ni en el panel del distribuidor ni en `Comisiones.js` del superadmin) — se construyó desde cero, inspirado en un formato de hoja de cálculo que el usuario ya usa para su contabilidad interna (secciones con subtotales + un "TOTAL DE COMISIONES" al final), adaptado a lo que el sistema sí guarda (no hay bancos/IVA/varias personas por venta en el modelo de distribuidores).
- **`assets/js/CSVExport.js`** (nuevo): helper compartido — antes `Cobros.js`/`Reportes.js`/`SuperReportes.js` reimplementaban cada uno su propio `exportarCSV` a mano, con una `data:` URI (límite de tamaño en algunos navegadores), sin separador de miles en los montos, sin comillas escapadas de verdad, y sin BOM (Excel en Windows a veces rompe acentos/ñ/el signo de peso sin esto — ver la plantilla de importación de `Alumnos.js`, que ya lo hacía bien). Se centralizó en `CSVExport.money(n)` (formato `$X,XXX.XX`) y `CSVExport.descargar(nombre, filas)` (Blob + BOM + comillas escapadas de verdad).
- **`acciones/superadmin_listar_referidos.php`**: antes solo devolvía el `%` de comisión de cada referido, nunca el monto real en pesos (eso solo existía en `distribuidor_comisiones.php`, y solo para el distribuidor dueño de la sesión). Se agregó el mismo cálculo (cobrado del mes/año × %) para TODOS los referidos activos vinculados a una escuela, sin importar el distribuidor — necesario para poder armar un total real en el export del superadmin.
- **`views/Distribuidor.js`** (`DistComisionesView`): botón "Exportar CSV" — Colegio, % Comisión, Cobrado del mes, Comisión del mes, Comisión acumulada, con fila de título (nombre del distribuidor + mes) y fila TOTAL al final.
- **`views/Comisiones.js`** (superadmin): botón "Exportar CSV" — mismas columnas pero **agrupado por distribuidor**, con subtotal por grupo y un "TOTAL DE COMISIONES" general al final, respetando el filtro de búsqueda activo (si se buscó un distribuidor específico, se exporta solo ese).
- De paso, se aplicó el mismo criterio (formato de pesos + fila TOTAL) a los tres exports de CSV que ya existían (`Cobros.js`, `Reportes.js`, `SuperReportes.js`), pidiéndolo el usuario explícitamente como "referencia" de estilo, no como copia literal de columnas.

### 5.3ak El export de comisiones pasó de CSV a "Excel con formato" (`ExcelExport.js`)
El usuario pidió que el export de comisiones se viera "bonito" como su plantilla de contabilidad (colores, negritas, bordes por sección). Un CSV es texto plano — no puede llevar NINGÚN estilo visual, sin importar cómo se arme; eso quedó explicado antes de tocar código. La solución sin agregar ninguna librería nueva al proyecto: una tabla HTML con estilos inline, guardada con extensión `.xls` — Excel la abre directo y respeta el formato como si fuera un `.xlsx` nativo (truco estándar, sin dependencias, coherente con que el proyecto no usa bundler).
- **`assets/js/ExcelExport.js`** (nuevo): `ExcelExport.descargar(nombreArchivo, filas)`, donde cada fila declara su propio estilo (`titulo`, `subtitulo`, `header`, `grupo`, `dato`, `subtotal`, `total`) — paleta tomada de las variables de marca ya usadas en toda la app (navy `#282d65` / lime `#bdcf00`), para que el archivo descargado se sienta del mismo sistema.
- **`views/Distribuidor.js`** (`DistComisionesView`), **`views/Comisiones.js`** (superadmin, agrupado por distribuidor), **`views/Cobros.js`** ("Historial de cobros") y **`views/Reportes.js`**: el botón "Exportar CSV" se reemplazó por "Exportar" en las cuatro vistas — mismas columnas y totales que 5.3aj, pero ahora con encabezados en negrita sobre fondo navy, fila de título, subtotales/agrupado donde aplica, y la fila de TOTAL en lime. El usuario pidió esto en dos pasadas (primero comisiones, luego "también en reportes y en historial de cobros") — quedó un solo botón "Exportar" por vista, sin CSV plano en paralelo.
- **`views/SuperReportes.js`** es el único que se quedó en CSV plano (con el formato de pesos + fila TOTAL de 5.3aj) — no se pidió explícitamente ahí; si hace falta, es el mismo patrón de las otras cuatro vistas, ver el código de cualquiera de ellas como referencia.
- **Bug real encontrado al probar en Excel de verdad**: las referencias de pago SPEI/CoDi/Efectivo (cadenas de hasta 30 dígitos) se veían en el archivo descargado como `7.77878E+28` (notación científica) o con los ceros a la izquierda recortados (`000000349863801` → `349863801`) — Excel "adivina" que cualquier celda que se vea como número ES un número, sin que nadie se lo pida, y con números tan largos pierde precisión de verdad (no es solo cosmético: la referencia queda irreconocible para conciliar el pago). Se corrigió agregando `mso-number-format:"\@"` (el código de Excel para forzar formato Texto) a TODAS las celdas de dato de `ExcelExport.js` — ninguno de estos reportes se piensa recalcular dentro de Excel (el TOTAL ya viene precalculado en la fila final), así que no había ninguna columna donde convenga arriesgarse a que Excel "numerice" el contenido.

### 5.3al Rediseño de `ExcelExport.js`: encabezados neutros, números alineados a la derecha, y color rotativo por grupo
El usuario mandó una captura de su plantilla de contabilidad YA formateada (no el CSV plano de antes) pidiendo ese mismo nivel de pulido en las cuatro vistas. Cambios concretos sobre el diseño de 5.3ak:
- **Encabezados de columna neutros** (blanco + borde gris oscuro, texto negro en negritas) en vez de navy — en la plantilla de referencia el color se usa para las franjas de sección y los totales, NO para los encabezados; tenerlos todos en navy competía visualmente con las franjas de sección y aplanaba la jerarquía.
- **Números alineados a la derecha**: como ahora todas las celdas se fuerzan a Texto (5.3ak), Excel ya no las alinea solo por ser "números" — se agregó una heurística en `celda()` (si el valor empieza con `$` o termina en `%`) para alinearlas a la derecha a mano, igual que cualquier hoja de cálculo real.
- **Paleta rotativa por grupo** (`PALETA`: navy → verde → magenta → ámbar, tomada de las variables de marca en `main.css`): en `Comisiones.js`, cada distribuidor ahora tiene su propia franja de color Y su propio renglón de total en ESE MISMO color — mismo lenguaje visual que la plantilla de referencia (Contabilidad en azul, Legal en verde, Marketing en magenta, cada una con su propio total a juego).
- **Se separó `total` de `granTotal`**: `total` (con `color: i`) es el total de UN solo grupo, a juego con su franja; `granTotal` es la suma de TODOS los grupos (o el único total, en las vistas sin agrupar como `Cobros.js`/`Reportes.js`/`Distribuidor.js`) y siempre es lime — la única fila que debe distinguirse de todas las franjas de color de arriba, para que no se pierda entre ellas.

### 5.3am Comprobante de pago en efectivo con marca propia + domiciliación (CAI) completada
El usuario mandó la documentación técnica completa de Cobroscontarjeta.com/Pagadetodo (liga de pago, referencias de efectivo, SPEI, CAI/domiciliación) y una imagen de referencia de un "Formato de Pago" de otro emisor (Club Pago), pidiendo (1) un comprobante propio para pagos en efectivo con el logo de la escuela, y (2) terminar la domiciliación de tarjeta (CAI). Antes de tocar nada se investigó a fondo qué ya existía — resultó ser mucho más de lo esperado:
- **La integración con Cobroscontarjeta.com ya existía casi completa** desde antes de esta sesión: `generar_liga.php`, `generar_referencia_efectivo.php`, `generar_clabe_individual.php`, `cobrar_cai.php`, `cancelar_cai.php`, y 8 webhooks públicos en `webhooks/`. **CAI específicamente ya tenía todo el backend construido pero estaba huérfano**: ninguna vista lo llamaba nunca, y la tokenización de tarjeta (`PLE_URL_LIGA_TOKEN`/`GenerarLigaDomiciliacionIndi`) llevaba tiempo apuntando al endpoint SIN tokenizar (`PLE_URL_LIGA_SIMPLE`) porque, según el propio comentario del código (con evidencia real en logs), el endpoint de domiciliación nunca devolvió un solo `code:success` en Sandbox pese a probar todos los formatos de `Reference` documentados — **no se tocó esa decisión**: revertir eso sin acceso a las credenciales de Sandbox arriesgaba romper el flujo de liga de pago simple, que sí funciona en producción.
- **Bug real encontrado y corregido**: `PortalFamilia.js` usaba `eliminar_tarjeta_guardada` (una acción que solo borraba el token en la BD local, sin avisarle al proveedor) en vez de `cancelar_cai` (que sí cancela la domiciliación real del lado de Cobroscontarjeta.com). Un padre que "eliminaba" su tarjeta desde el portal dejaba el token vivo del lado del proveedor aunque aquí ya dijera "cancelada". Se cambió el frontend para usar `cancelar_cai`, y se borró `eliminar_tarjeta_guardada.php` (quedó sin ningún otro call-site).
- **Bug real encontrado y corregido**: `cobrar_cai.php` marcaba el cobro como `pagado` pero nunca llamaba a `recalcular_saldo_pendiente()` — mismo patrón de bug que 5.3 (Fase 1 de saldo_pendiente), solo que en un archivo que no estaba en la lista original porque nunca se había ejecutado en producción (nadie lo llamaba).
- **`curl_post()`/`log_api()` se movieron de `api.php` a `lib/curl_helper.php`** (nuevo) — vivían solo ahí, así que ningún script fuera del ciclo HTTP (como `cron_recordatorios.php`, que corre por CLI) podía llamar a un proveedor de pago externo. `api.php` ahora solo los requiere.
- **Nueva función compartida `cobrar_via_token()`** en `lib/helpers_pagos.php` — el payload/curl/parseo de respuesta de un cargo CAI, extraído de `cobrar_cai.php` para que `cron_recordatorios.php` lo reutilice sin duplicar la llamada al proveedor.
- **Nueva sección 0.5 en `cron_recordatorios.php`**: cobro automático diario de cualquier cobro recurrente pendiente cuyo alumno tenga `token_tarjeta_estado='activo'` — reintenta cada día que corra el cron hasta que se pague o se cancele la tarjeta. **Hoy no cobra a nadie** (ver el bloqueo de tokenización arriba: nunca hay un `token_tarjeta` poblado) pero queda listo y sin costo — empezará a funcionar solo, sin más cambios de código, en cuanto Cobroscontarjeta.com resuelva su Sandbox.
- **Nuevo botón "Tarjeta guardada" en Caja.js**: aparece como método de pago solo cuando el alumno seleccionado ya tiene `token_tarjeta_estado='activo'` — cobra directo con `cobrar_cai`, sin modal de espera (a diferencia de TC/SPEI/Efectivo, es una respuesta síncrona del proveedor, no un pago que el cliente completa después).
- **`con_cai` dejó de ser código muerto**: `generar_liga.php` nunca mandaba este campo en su respuesta, así que el aviso "Domiciliación no disponible en este momento" en `Caja.js` (ya escrito, ya traducido, nunca visible) jamás se mostraba porque `undefined !== false` se evalúa `true`. Se agregó `'con_cai' => false` explícito a la respuesta — el aviso ahora sí aparece, y es información precisa (hoy ningún pago con tarjeta deja el token guardado).
- **Comprobante de pago en efectivo con marca propia** (`views/Caja.js`, función `abrirComprobanteEfectivo`): reemplaza el botón que abría el PDF genérico hospedado por el proveedor (con el logo de Cobroscontarjeta/Pagadetodo) por una página HTML propia, generada en el navegador con `window.open()` — con el logo real de la escuela (`escuelas.logo_url` o `assets/logo.jpeg`), monto con importe en letras (nueva función `numeroALetras`, conversión número-a-texto en español), el código de barras que ya regresa el proveedor (`ref_barcode_url`, se muestra tal cual, no se regenera), instrucciones genéricas de pago (sin inventar una red de tiendas específica ni reproducir logos de cadenas comerciales ajenas) y un botón "Imprimir / Guardar como PDF" (`window.print()`, con `@media print` para ocultar los elementos que no deben imprimirse). Mismo criterio que `ExcelExport.js`: no hay ninguna librería de PDF en el proyecto, así que el mecanismo es una página HTML autocontenida pensada para imprimirse/guardarse desde el propio navegador.

### 5.3an Domiciliación (CAI) confirmada por el proveedor + arreglos tras probarla en vivo
Tras 5.3am, el usuario probó de verdad el endpoint de tokenización en el Sandbox de Cobroscontarjeta.com y confirmó que **ya funciona** (`code:success`, mismo formato de `Id`/`Reference` que el simple). Se le preguntó explícitamente si CUALQUIER pago con tarjeta debía domiciliarse en automático o si debía pedirse consentimiento explícito primero — eligió lo segundo.
- **`acciones/generar_liga.php` migrado a `PLE_URL_LIGA_TOKEN`** (`GenerarLigaDomiciliacionIndi`) — ya no usa el endpoint simple. La respuesta ahora manda `'con_cai' => true` (antes eran `false`/ausente): cualquier pago con tarjeta que el proveedor apruebe deja el token guardado, la API no tiene un interruptor por transacción para evitarlo.
- **`views/PortalFamilia.js`**: nueva casilla obligatoria "Autorizo que mi tarjeta quede guardada..." antes de poder pagar con tarjeta nueva — es el consentimiento explícito que se pidió, ya que la tokenización ya no se puede evitar del lado del proveedor.
- **Bug real encontrado al probarlo con reintentos**: el usuario reportó que al abandonar el formulario de pago y volver a intentar, el proveedor respondía "La referencia es única e irrepetible" (su código de error 23). La `Reference`/`Id` se armaban con `time()` (resolución de 1 segundo) + `mt_rand()` de 3 dígitos — insuficiente entropía entre reintentos rápidos. Se reemplazó por `microtime()` real (resolución de microsegundos) + `random_int` criptográfico, mismo formato de dígitos ya confirmado con el proveedor (9/15, con ceros a la izquierda, como string). **Nota: este fix quedó reemplazado por 5.3ao** — otra sesión resolvió el mismo síntoma con un enfoque distinto (`construir_referencia_pago()`, numeración secuencial por alumno) antes de que este se fusionara; ver 5.3ao para el bug real que aún tenía ESE enfoque.
- **Bug real encontrado (mismo patrón que 5.3/Fase 1 de `saldo_pendiente`)**: `cobrar_cai.php` marcaba el cobro como pagado pero nunca recalculaba `clientes.saldo_pendiente`. Se resolvió extrayendo la lógica de cobro a una función compartida **`cobrar_via_token()`** en `lib/helpers_pagos.php` (payload/curl/parseo de respuesta + el recálculo de saldo), reutilizada tanto por `cobrar_cai.php` como por la nueva sección 0.5 de `cron_recordatorios.php` (cobro automático diario de recurrentes con tarjeta domiciliada activa) — antes esa lógica solo vivía duplicada a mano.
- **`curl_post()`/`log_api()` se movieron a `lib/curl_helper.php`** (nuevo, requerido por `api.php`) para que `cron_recordatorios.php` (que corre por CLI, no por HTTP) también pueda llamar al proveedor.
- **Mejora de UX pedida por el usuario**: "si ya tienes domiciliada la tarjeta, ¿para qué volver a pedirla?" — se agregó una opción adicional de método de pago **"Tarjeta guardada"** en `PortalFamilia.js`, que solo aparece cuando el alumno seleccionado ya tiene `token_tarjeta_estado='activo'` (mismo patrón ya usado en `Caja.js`). Cobra directo vía `cobrar_cai` (respuesta síncrona, sin checkbox de consentimiento — esa tarjeta ya se autorizó en un pago anterior, sin redirigir a ninguna liga externa). El diseño es explícitamente **por alumno, no por familia**: cada hijo tiene su propio `token_tarjeta` en `clientes`, a propósito — la tarjeta de un hermano nunca se ofrece para cobrar el saldo de otro.

### 5.3ao Repo fusionado con otra sesión trabajando en paralelo en lo mismo (CAI) + bug real en `construir_referencia_pago()`
Al preguntar "¿has traído los cambios actuales del repo aquí?", se encontró que `origin/main` había avanzado 20+ commits que el working local no tenía — otra sesión (u otra persona) llevaba tiempo trabajando en paralelo en domiciliación/CAI, con commits tipo "Fix response structure in cobrar_cai.php", "Refactor card cancellation logic for shared tokens", aviso previo por correo antes de un cargo automático (`CAI_DIAS_AVISO`), y — clave — una reescritura completa de la generación de referencia (`construir_referencia_pago()` en `lib/helpers_pagos.php`, numeración secuencial 9+4 dígitos por alumno según la doc del proveedor) que reemplazaba el parche de 5.3an. Se hizo `git pull`: un solo conflicto real (`acciones/generar_liga.php`, la misma línea que ambos lados habían tocado) — se resolvió a favor de `construir_referencia_pago()` por seguir el formato documentado en vez de solo reforzar entropía. Todo lo demás fusionó limpio, incluyendo el botón "Tarjeta guardada" de Portal Familia (5.3an), que el otro lado nunca tocó. **Aparte, se confirmó con el usuario que el SMTP a `seguroslux.mx` en `config.php` es intencional** (no una filtración/tampering).
- **Bug real encontrado (reportado tal cual por el usuario): "pago, salgo, reintento — la 2ª o 3ª vez falla con 'la referencia es única e irrepetible'"**. `construir_referencia_pago()` calculaba el consecutivo contando `SELECT COUNT(*) FROM cobros WHERE cliente_id=? AND referencia IS NOT NULL AND referencia<>''` — pero `generar_liga.php`/`cobrar_via_token()` **sobrescriben** la `referencia` del MISMO cobro pendiente en cada reintento (no insertan una fila nueva), así que ese conteo nunca avanzaba más allá de 1: cada reintento recalculaba una referencia que YA se le había mandado al proveedor en un intento anterior — localmente "olvidada" al sobrescribirse, pero el proveedor la recuerda para siempre y la rechaza. Se reemplazó la base del consecutivo por `time()` (nunca se "olvida" ni se puede repetir sin importar cuántas veces se sobrescriba `cobros.referencia` localmente), dejando el ciclo de reintento local ya existente como respaldo. Mismo formato de dígitos, sin cambio de esquema de BD.

### 5.3ap Respuestas a una revisión técnica de CAI (referencia con código de barras, evidencia de pago, notificaciones, cancelación de token, CLABE SPEI)
Alguien revisó la integración y mandó 6 preguntas/comentarios; se verificó cada uno directo en el código actual (no de memoria) antes de responder:
1. **Código de barras en el comprobante de efectivo**: el código YA está listo para mostrarse (`Caja.js`, `abrirComprobanteEfectivo`, usa `ref_barcode_url` tal cual lo regrese el proveedor) — pero casi seguro no aparece porque la cuenta de Cobroscontarjeta.com sigue en la configuración por default ("opción 1: solo referencia en texto", según su propia doc). Hay que pedirles activar la opción 2 (referencia + código de barras) para esta cuenta — no es un bug de este lado.
2. **CAI manual vs. automático**: ambos existen — botón "Tarjeta guardada" (Caja.js y PortalFamilia.js) para manual, y una sección del cron diario para automático (con aviso previo y posterior por correo).
3. y 4. — resueltos en esta pasada, ver 5.3aq.
5. **Cancelación de token**: sí se elimina de verdad (`NULL`, no solo cambio de estado) — y de paso cubre el caso de hermanos que comparten tarjeta (mismo token del lado del proveedor): cancelar en uno cancela en todos los que lo compartían, para que no le falle el siguiente cargo automático al otro.
6. **CLABE SPEI**: existe un comprobante separado (`abrirComprobanteSPEI`) con CLABE/banco/beneficiario — el comprobante de efectivo/referencia NO la incluye como alternativa dentro del mismo documento (a diferencia de la plantilla de referencia que compartió el usuario, que traía ambas opciones juntas). Quedó pendiente, no se pidió explícitamente en esta pasada.

### 5.3aq Evidencia de pago con tarjeta completada + correo de confirmación centralizado (puntos 3 y 4 de 5.3ap)
- **Nuevas columnas** (`migracion_2026_08_28_evidencia_pago_tarjeta.sql`, correr una vez en phpMyAdmin): `cobros.cc_mask`/`cc_type`/`pago_email`, `clientes.token_tarjeta_mask`/`token_tarjeta_tipo`. Antes esta evidencia (qué tarjeta, terminada en qué dígitos) se descartaba por completo al no haber dónde guardarla.
- **`webhooks/webhook_liga.php`**: ahora lee `cc_mask` (con `cc_number` como respaldo, por si el proveedor solo manda ese campo en la práctica), `cc_type` y `email` del webhook, y los guarda tanto en el cobro como — si trae `number_tkn` — en el cliente que quedó domiciliado.
- **`lib/helpers_pagos.php`, `cobrar_via_token()`**: ahora también guarda `cc_mask`/`cc_type` (leídos de `txResponse.cc_number`/`cc_type`, el nombre que usa la doc para este endpoint específico) en cada cargo automático o manual.
- **Correo de confirmación centralizado**: antes solo lo mandaba `cron_recordatorios.php` (cargo automático) — un cobro manual con "Tarjeta guardada" (Caja.js o PortalFamilia.js) no avisaba a nadie. Se movió el envío DENTRO de `cobrar_via_token()` (con una consulta propia de nombre/correo del cliente o su familia), para que ambos caminos avisen por igual sin duplicar el texto del correo en dos archivos — se quitó el bloque de correo que tenía el cron, ya redundante.
- **Evidencia de que el correo de verdad se mandó**: `enviar_correo()` solo deja rastro en `CORREOS_LOG_FILE` cuando FALLA — un envío exitoso no dejaba ninguna línea en ningún lado. Se agregó un log explícito en `cobrar_via_token()` (`log_api()`, línea "correo de confirmación a ... -> OK/FALLÓ") para poder comprobar el envío sin depender de revisar la bandeja de alguien más.

### 5.3ar Autorización firmada para Cargos Automáticos (DocuSign) — el checkbox de "acepto términos" no es válido para el banco
Un tercero (relacionado con Cobroscontarjeta.com) le avisó al usuario que los cargos automáticos con tarjeta requieren una **autorización firmada de verdad** (autógrafa, o por una plataforma de firma electrónica legalmente válida) — un simple checkbox de "acepto/autorizo" (lo que ya se había construido en 5.3an) **no es aceptado**, confirmado porque ya le habían rechazado pagos así antes. El usuario eligió DocuSign como proveedor de firma; todavía no tiene cuenta — se le dio un checklist de qué crear en developers.docusign.com (Integration Key, llave RSA, User ID, Account ID, dar consentimiento una vez) y se dejó todo el sistema construido y listo para conectar en cuanto existan credenciales reales. **Nada de esto se ha podido probar en vivo** — está escrito correcto contra la documentación pública de DocuSign (REST API v2.1, autenticación JWT Grant), pero falta la prueba de punta a punta.
- **Hallazgo clave antes de tocar nada**: como `generar_liga.php` usa el endpoint de dominiciliación de Cobroscontarjeta.com de forma incondicional (ver 5.3an), CUALQUIER pago con tarjeta exitoso tokeniza la tarjeta sin que exista ningún interruptor por transacción del lado del proveedor — un checkbox en el frontend no evita que esto pase. Por eso el candado real no puede vivir solo en la pantalla: tiene que estar en el momento de **cobrar** con esa tarjeta guardada.
- **`migracion_2026_08_28_autorizacion_firmada_cai.sql`** (correr una vez en phpMyAdmin): agrega a `clientes` — `autorizacion_cai_estado` (`sin_solicitar`/`enviada`/`firmada`/`rechazada`, default `sin_solicitar`), `autorizacion_cai_envelope_id`, `autorizacion_cai_fecha_envio`, `autorizacion_cai_fecha_firma`, `autorizacion_cai_documento_url`. Con el default en `sin_solicitar`, **ningún alumno que ya tuviera tarjeta domiciliada de antes de este cambio puede cobrarse hoy** hasta que se le mande y firme la autorización — comportamiento correcto a propósito.
- **Candado real, en `lib/helpers_pagos.php`, `cobrar_via_token()`**: antes de cobrar, verifica `clientes.autorizacion_cai_estado = 'firmada'`; si no, rechaza con un mensaje claro. Como esta función es el único punto por el que pasan TANTO el cobro manual (botón "Tarjeta guardada" en Caja.js/PortalFamilia.js) COMO el automático (cron), poner el candado aquí protege los dos caminos con un solo cambio, sin depender de que cada pantalla lo valide bien por su cuenta.
- **`lib/docusign_helper.php`** (nuevo): autenticación JWT Grant (arma y firma el JWT con `openssl_sign`, sin librería externa — mismo criterio que `mailer.php`, que tampoco usa ninguna), resuelve el `base_uri`/`account_id` reales vía `/oauth/userinfo` (no se asume un host fijo), y `docusign_enviar_autorizacion_cai()` arma la carta (HTML, DocuSign la convierte a PDF solo) y crea el sobre con un firmante. El texto de la carta (`docusign_carta_autorizacion_html()`) es una redacción razonable, **no una revisión legal** — antes de usarse para autorizaciones reales, que alguien con criterio legal en México la revise.
- **`acciones/iniciar_firma_cai.php`** (nuevo, autenticado): dispara el envío del sobre para un alumno — mismo patrón de pertenencia que `cancelar_cai.php` (familia solo su hijo, admin/cajero solo su escuela, superadmin cualquiera).
- **`webhooks/webhook_docusign.php`** (nuevo, público): recibe la notificación de DocuSign Connect cuando el sobre se completa/rechaza/anula y actualiza `autorizacion_cai_estado`. Incluye verificación HMAC opcional (`DOCUSIGN_CONNECT_HMAC_KEY`) — recomendado activarla en DocuSign Admin al configurar Connect. **El formato exacto del payload de Connect depende de su configuración** (elegir JSON, no XML, al darla de alta) — si al probarlo en vivo el formato real no calza, revisar `webhooks/debug_webhook_docusign.txt` (se guarda el payload completo tal cual llega) y ajustar, mismo patrón que ya se usó en `webhook_liga.php` para su caso de la referencia envuelta.
- **`config.php`**: nuevas constantes `DOCUSIGN_INTEGRATION_KEY`/`DOCUSIGN_USER_ID`/`DOCUSIGN_ACCOUNT_ID`/`DOCUSIGN_PRIVATE_KEY`/`DOCUSIGN_AUTH_HOST`/`DOCUSIGN_CONNECT_HMAC_KEY`, todas vacías por ahora — con el checklist de qué hacer en DocuSign para llenarlas, en comentario justo ahí.
- **`views/PortalFamilia.js`**: se quitó el checkbox "Autorizo que mi tarjeta quede guardada..." por completo. En su lugar, al pagar con tarjeta se muestra el estado real (`autorizacion_cai_estado`): si ya está `firmada`, un aviso verde y se puede pagar normal; si no, un botón "Firmar autorización" (o "Reenviar" si ya se mandó) que llama a `iniciar_firma_cai` — el botón de pagar con tarjeta queda deshabilitado hasta que el webhook confirme la firma.
- **Pendiente, no se hizo en esta pasada**: `Caja.js` no tiene ningún punto para disparar `iniciar_firma_cai` desde ahí (un cajero solo ve el error real de `cobrar_via_token()` si lo intenta sin firma) — se puede agregar un botón equivalente ahí si hace falta que el cajero también pueda iniciarlo por el padre presente en caja.

### 5.3as Se pausó la implementación de 5.3ar — queda solo el esqueleto, sin conectar
A petición del usuario, se revirtió la parte ACTIVA de 5.3ar (el candado y la UI), dejando los archivos nuevos como esqueleto sin usar para retomarlo más adelante:
- **`lib/helpers_pagos.php`, `cobrar_via_token()`**: se quitó el candado que exigía `autorizacion_cai_estado = 'firmada'` — vuelve a cobrar con solo `token_tarjeta_estado = 'activo'` (el comportamiento de antes de 5.3ar). El comentario que queda ahí explica cómo reactivarlo.
- **`views/PortalFamilia.js`**: se quitó el botón "Firmar autorización"/aviso de estado y se **restauró el checkbox** "Autorizo que mi tarjeta quede guardada..." de 5.3an (sigue siendo el mecanismo de consentimiento que un tercero avisó que el banco rechaza — se restauró tal cual porque el usuario pidió pausar la firma real, no porque el problema de fondo se haya resuelto).
- **Lo que SÍ queda, sin conectar a nada** (esqueleto para cuando se retome): `migracion_2026_08_28_autorizacion_firmada_cai.sql` (no se corrió), `lib/docusign_helper.php`, `acciones/iniciar_firma_cai.php`, `webhooks/webhook_docusign.php`, y las constantes `DOCUSIGN_*` en `config.php` (todas vacías). Ninguno de estos archivos se llama desde ningún otro lado del código ahora — están inertes.
- **Importante para quien retome esto**: el problema de fondo (un checkbox no es una autorización válida para el banco) sigue sin resolverse — quedó documentado en 5.3ar/5.3aq para cuando se decida continuar.

### 5.3at Bug real en `construir_referencia_pago()`: un intento fallido nunca "quemaba" la referencia, y el proveedor sí la recordaba
El usuario reportó `test_domiciliacion.php` (script de diagnóstico ya existente) fallando con `{"Message":"Error."}` HTTP 500, y pegó un fragmento real de `api_log.txt` que reveló DOS problemas mezclados:
1. **Bug real, corregido aquí**: el 2026-08-28, `generar_liga` reintentó la referencia `000000000020066` varias veces seguidas (incluso para un folio distinto) y todas fallaron con código 23 "La referencia es única e irrepetible". Causa: tanto `generar_liga.php` como `cobrar_via_token()` solo guardaban `cobros.referencia` DESPUÉS de una respuesta exitosa del proveedor — si el intento fallaba, la referencia nunca se persistía, así que `construir_referencia_pago()` (que evita repetir solo referencias YA guardadas en `cobros`) volvía a calcular y proponer la MISMA referencia en el siguiente intento. El proveedor sí la recuerda desde la primera vez que se le manda, sin importar si nuestro lado la "olvidó". **Fix**: en ambos archivos, `UPDATE cobros SET referencia = ?` ahora corre INMEDIATAMENTE después de generar la referencia, antes de llamar al proveedor — así un intento fallido igual "quema" esa referencia y el siguiente cálculo salta a la que sigue.
2. **No es un bug nuestro**: en paralelo, desde el 2026-09-02 tanto `generar_liga` como `cobrar_via_token` fallan con `{"Message":"Error."}` (HTTP 500, respuesta genérica sin `code`) — se confirmó reproduciendo `test_domiciliacion.php` en vivo. El payload enviado es correcto (`IntegrationID:125`, el que corresponde mientras `PLE_HOST_BASE` siga en `pagadetodo.mx` — se verificó explícitamente que NO hay un mismatch de ID). Mientras tanto, `generar_referencia_efectivo` (mismo dominio `pagadetodo.mx`, distinto endpoint) siguió funcionando sin problema todos esos días — esto aísla la falla específicamente al servicio de Pagalaescuela/Domiciliación del lado de Cobroscontarjeta.com, no a nuestro código. Hay que reportárselo al proveedor con la evidencia (timestamps + payload + respuesta cruda del log).

### 5.3au Cambio de dominio: `test.grupoideasmx.com` → `sistemasedupago.com`
- Mismo hosting (Hostinger), solo cambia qué dominio apunta a la cuenta — confirmado con el usuario antes de tocar `DB_HOST`.
- Cambiado en `config.php`: `DB_HOST` (línea 14), `WEBHOOK_URL` (línea 119), `WEBHOOK_LIGA_URL` (línea 127). También actualizado `Config.example` para que la plantilla quede consistente.
- **No requirió tocar nada más.** `APP_URL`/`APP_ALLOWED_ORIGIN` nunca están `define()`'dos en este proyecto — todo el código que arma links (activación, invitaciones, reset de password) y el CORS los resuelve dinámicamente del propio request, así que se adaptan solos a cualquier dominio que sirva la app. El `.htaccess` (CSP) solo referencia `'self'` + CDNs fijos de terceros, no el dominio propio. Los dominios de Cobroscontarjeta.com (`pagalaescuela.mx`/`pagadetodo.mx`) son del proveedor y no se tocan.
- **Pendiente, fuera de este repo**: `WEBHOOK_URL`/`WEBHOOK_LIGA_URL` son solo informativos (nada en el código los lee — ver nota histórica más abajo); lo que de verdad importa es actualizar el "EndPoint" registrado en el panel de Cobroscontarjeta.com para que apunte a `https://sistemasedupago.com/webhooks/webhook_liga.php` (y el resto de webhooks bajo el nuevo dominio) — si no se actualiza ahí, sus notificaciones seguirán intentando llegar al dominio viejo.
- También pendiente: apuntar el DNS de `sistemasedupago.com` a la misma cuenta de Hostinger (A record o el nameserver que Hostinger indique) y, si aplica, emitir/activar el certificado SSL para el dominio nuevo antes de dar de baja `test.grupoideasmx.com`.

### 5.3av Limpieza de código muerto (barrido verificado, sin tocar el esqueleto de DocuSign)
A petición del usuario ("quita las cosas y limpia el código, que ya no se ocupen"), se corrió un barrido en 6 áreas del proyecto en paralelo, seguido de una reverificación adversarial de cada hallazgo antes de borrar nada (para no repetir el error de confundir un archivo de `acciones/*.php` — que se despacha dinámicamente por `$action`, nunca por un `require` literal — con código muerto).

**Eliminado:**
- `test_ip_publica.php` y `test_db_localhost.php`: scripts de diagnóstico de un solo uso ya usados esta semana (confirmar IP pública de salida; comparar `DB_HOST=localhost` vs el valor real), cada uno con su propio docblock pidiendo borrarlo tras usarse. Ninguno tenía guardia CLI, así que quedaban ejecutables por URL directa mientras existieran.
- `controllers/ProductoController.js`: versión singular obsoleta, reemplazada por `controllers/ProductosController.js` (plural). No aparecía en ningún `<script src>` de `index.html`/`registro.html`/`activar_cuenta.html` ni referenciado por ningún otro archivo.
- Constantes huérfanas en `config.php`/`Config.example` (cero referencias fuera de su propio `define()`, confirmado por grep en todo el proyecto): `SPEI_CLABES_FILE` (apuntaba a un `clabes_alumnos.json` que nunca se lee/escribe), `WEBHOOK_URL` y `WEBHOOK_LIGA_URL` (ya documentadas arriba como puramente informativas — el webhook real se da de alta en el panel de Cobroscontarjeta.com/Pagadetodo, nada en el código las lee), `ADMIN_EMAIL` (el super-admin real se identifica por cuenta en BD, no por esta constante), `MAIL_FROM_EMAIL`/`MAIL_FROM_NAME` (remanente de una implementación con `mail()` nativo, reemplazada por el bloque SMTP que usa `SMTP_FROM_EMAIL`/`SMTP_FROM_NAME`), y todo el bloque `PAC_API_URL`/`PAC_USER`/`PAC_PASS` (facturación con Facturama, migrada hace tiempo a Facturapi — `lib/facturapi.php`/`FACTURAPI_KEY`).

**Revisado y conservado a propósito (no son basura, aunque el barrido inicial dudó de algunos):**
- `test_domiciliacion.php`: sigue siendo la única herramienta para reproducir el HTTP 500 activo de Cobroscontarjeta.com en Domiciliación (ver 5.3at) — no hay confirmación de que el proveedor ya lo haya resuelto.
- `diagnostico_smtp.php`: se autoprotege (`if (php_sapi_name() !== 'cli') { ... 403 ...}`), no es explotable por URL, y su historial de commits (creación + refactor) lo confirma como herramienta reutilizable, no un diagnóstico de incidente puntual.
- `PLE_URL_LIGA_SIMPLE` (config.php): es el fallback deliberado para el mismo incidente activo de Domiciliación (error 500 recurrente) — no es código huérfano de una migración incompleta.
- `migracion_2026_08_28_evidencia_pago_tarjeta.sql`: no hay confirmación en este documento de que ya se haya corrido en producción — pendiente de que el usuario lo verifique en phpMyAdmin.
- Los 5 archivos de log versionados en la raíz (`error_log`, `api_log.txt`, `webhook_log.txt`, `referencias_log.txt`, `debug_webhook.txt`): son evidencia operativa viva, no código muerto. El repo no tiene `.gitignore`, así que quedan versionados por defecto — decisión del usuario si conviene archivarlos/ignorarlos hacia adelante.
- El esqueleto de DocuSign completo (constantes `DOCUSIGN_*`, `lib/docusign_helper.php`, `acciones/iniciar_firma_cai.php`, `webhooks/webhook_docusign.php`, `migracion_2026_08_28_autorizacion_firmada_cai.sql`): excluido a propósito de todo el barrido, por ser el esqueleto intencional documentado en 5.3as.

### 5.3aw Producción activada: credenciales reales de Cobroscontarjeta.com (2026-09-08)
Cobroscontarjeta.com certificó las URLs productivas (ver 5.3au/mensaje enviado con `https://sistemasedupago.com/...`) y entregó credenciales de producción para ambas plataformas.

**Aplicado en `config.php`:**
- `PDT_BUS_ID_SPEI`/`PDT_BUS_ID_TC`: `000002` (sandbox) → `000067` (producción, Pagadetodo — SPEI/Referencias).
- `PLE_SCHOOL_ID`: `000002` (placeholder) → `000041` (producción, Pagalaescuela — Pago en línea/CAI).
- `PDT_INT_ID` (125) y `PLE_INT_ID` (106) no cambiaron — el correo confirma que son los mismos IDs que ya estaban configurados.
- `PLE_HOST_BASE`: de `https://pagadetodo.mx/Pagadetodo` (workaround temporal desde el aviso de Cobroscontarjeta.com del 18-ago-2026) → `https://pagalaescuela.mx/Pagalaescuela` (producción real). Este era exactamente el cambio que el propio comentario del código ya anticipaba.
- `PDT_USER`/`PDT_PASS` (y por extensión `PLE_USER`/`PLE_PASS`, que se definen como el mismo valor): **pendientes** — el correo trae las credenciales productivas reales, compartidas para ambas plataformas, pero aún no se han cargado en `config.php`.

**Riesgo a vigilar en el primer cobro real:** el formato de `Reference` de 15 dígitos (`construir_referencia_pago()` en `lib/helpers_pagos.php`) se determinó empíricamente probando contra el Sandbox de **pagadetodo.mx** (27-ago-2026, código 22 con 13 dígitos). La documentación original del proveedor (mayo 2022) describe **pagalaescuela.mx**, que es justo el dominio real que ahora está activo — no se ha vuelto a probar el formato contra el validador de producción real. Si el primer `generar_liga`/`cobrar_via_token` en vivo regresa código 22 ("formato de la referencia incorrecto"), revisar ahí primero.

**Pendiente antes de operar en vivo:** cargar `PDT_USER`/`PDT_PASS` reales en `config.php` (no se pegaron en el chat por seguridad) y subir el archivo al servidor.

### 5.3ax Primeros cobros reales en producción: el riesgo de 5.3aw se confirmó (Liga/CAI), y un problema aparte en Referencias
El mismo día que se activó producción (08-sep-2026), los primeros cobros reales fallaron. Con el log real del servidor (`api_log.txt`, no la copia local que estaba desactualizada) se aislaron dos problemas distintos y no relacionados:

**1. Liga/CAI — código 22 "El formato de la referencia es incorrecto" — bug real, corregido.**
Confirmado exactamente el riesgo anotado en 5.3aw: `construir_referencia_pago()` mandó una Reference de 15 dígitos (`"000000000003982"`) contra `pagalaescuela.mx` en producción real, y el proveedor la rechazó. La doc original (mayo 2022, IntegracionesCAI_V1_1) especifica 13 dígitos para pagalaescuela.mx — el 15 era una necesidad específica del Sandbox temporal de pagadetodo.mx. **Fix**: nueva constante `REFERENCIA_DIGITOS` en `config.php`, calculada automáticamente según `PLE_HOST_BASE` (13 en pagalaescuela.mx/producción, 15 si algún día se vuelve a pagadetodo.mx) — mismo patrón que `PLE_INT_ID_ACTIVO`/`PLE_SCHOOL_ID_ACTIVO`. `construir_referencia_pago()` ya leía esta constante si existía (`defined('REFERENCIA_DIGITOS') ? ... : 15`), solo faltaba definirla.

**2. Referencias en efectivo — "No se pudo obtener la información del subEmisor (0000251888840043)" — NO es un bug de código, hay que reportarlo a Cobroscontarjeta.com.**
Este flujo (`PDT_URL_REFERENCIA`) siempre apunta a `pagadetodo.mx` de forma fija — no usa el toggle de `PLE_HOST_BASE`, así que no le aplica el mismo problema de largo de referencia que a Liga. Lo único que cambió aquí al pasar a producción fue el `BusinessID` (`000002` → `000067`). El mensaje de error apunta a que el proveedor no puede resolver un "subEmisor" asociado — lo más probable es que el `BusinessID` 000067 de producción todavía no esté vinculado del lado de Cobroscontarjeta.com específicamente para el servicio de Referencias en efectivo (aunque sí funcione para otros servicios). Hay que reportarles el error exacto (con folio `4463901` y el payload) y pedirles que confirmen la vinculación.

**3. Anomalía a investigar aparte, NO resuelta aquí: entradas "⚠ LIGA HUÉRFANA" con `response:approved`.**
El mismo log muestra varias notificaciones de `webhook_liga.php` el mismo día con `response:approved` (el banco/tarjeta SÍ aprobó el cargo) pero marcadas como huérfanas (ninguna referencia en `cobros` hace match). Sus referencias (ej. `0000120017549100011003`) no coinciden con ninguno de los 3 intentos fallidos de arriba, así que no se investigó su causa en esta pasada — pero si corresponden a cargos reales aprobados por el banco que el sistema no logró conciliar contra ningún cobro, es dinero real cobrado a un padre de familia sin quedar registrado como pagado. Vale la pena que el usuario revise esos `folio_cct` (624905207, 624916117, 624941090, 624944765) directamente con Cobroscontarjeta.com o en el estado de cuenta de la terminal para confirmar si son cargos reales y, si lo son, buscar manualmente a qué cobro correspondían.

### 5.3ay Confirmado contra la doc oficial: referencia de 13 dígitos correcta; PaymentTypes/subEmisor bloqueados por el proveedor
El usuario compartió los 5 PDFs originales de Cobroscontarjeta.com. Verificación cruzada:
- **Reference de Liga/CAI = 13 dígitos**: confirmado textual en IntegracionesCAI V1.1 pág. 4 ("9 dígitos alumno + 4 dígitos pago" = 13). El fix de 5.3ax/`REFERENCIA_DIGITOS` es correcto.
- **PaymentTypes '401' es SOLO de Sandbox, documentado así por el propio proveedor** (misma página, nota debajo de la tabla). La doc nunca especifica el código de producción — hay que pedírselo directamente a Cobroscontarjeta.com. Sin ese valor, `generar_liga`/CAI en producción seguirá fallando con `BAD_REQUEST`.
- **El error "subEmisor" (efectivo) no es un código documentado**: revisados los 20 códigos de error de `GenerarReferenciaIndi` (IntegracionesReferencias V1.4 pág. 9) — ninguno coincide. Confirma que es un mensaje ad-hoc de su lado, probablemente ligado a que BusinessID 000067 no está vinculado para el servicio de Referencias específicamente.
- **Hallazgo nuevo, no implementado**: `ConsultaEstatusPagosIntegraciones V1.1.pdf` documenta un endpoint `ConsultarEstatusLigaIndi` (`https://pagalaescuela.mx/Pagalaescuela/Service/ConsultarEstatusLigaIndi`) para consultar el estatus real de una liga por su Reference — no se usa en el código actual. Podría ser útil para reconciliar manualmente los casos "LIGA HUÉRFANA" con `response:approved` sin match (ver 5.3ax punto 3), en vez de depender solo del webhook entrante.

### 5.3az PaymentTypes de producción confirmado por Cobroscontarjeta.com: '41' (no '401')
Respuesta del proveedor (08-sep-2026) al reporte de 5.3ay: el `PaymentTypes` correcto para "Contado" en producción es **41** (el 401 es exclusivo de Sandbox, tal como decía su doc). Sobre el error de "subEmisor" en efectivo: van a revisar si el comercio (BusinessID 000067) quedó bien vinculado — sigue pendiente de su lado.

**Aplicado**: nueva constante `PLE_PAYMENT_TYPES` en `config.php`, calculada automáticamente según `PLE_HOST_BASE` (mismo patrón que `REFERENCIA_DIGITOS`/`PLE_INT_ID_ACTIVO`) — `'41'` en producción (pagalaescuela.mx), `'401'` si algún día se vuelve a usar el workaround de Pagadetodo. `acciones/generar_liga.php` y `test_domiciliacion.php` ahora usan esta constante en vez del `'401'` hardcodeado. También se corrigió `test_domiciliacion.php` para usar `REFERENCIA_DIGITOS` en vez de 15 fijo (mismo bug de 5.3ax, ya se le había escapado a este script).

**Pendiente**: confirmar con un cobro real que Liga/CAI ya funciona en producción con `PaymentTypes=41`. El caso de "Referencias en efectivo" (subEmisor) sigue bloqueado del lado del proveedor.

### 5.3ba Efectivo sigue bloqueado en producción: BusinessID cambiado a 000002 por indicación del proveedor, ahora código 404 (permisos)
Cobroscontarjeta.com pidió (09-sep-2026) regresar el BusinessID de Referencias en efectivo a `000002` (distinto del de SPEI/TC, que sigue en `000067`) mientras investigaban el error de "subEmisor" de 5.3ay/5.3az. Aplicado en `config.php`: `PDT_BUS_ID_EFECTIVO` ahora es `'000002'` fijo, ya no se calcula igual a `PDT_BUS_ID_SPEI`.

Con ese cambio, el error dejó de ser el mensaje libre de "subEmisor" y pasó a ser código **404** — documentado en IntegracionesReferencias V1.4: "No tiene permiso para generar las formas de pago, por favor comuníquese con los administradores." Sigue siendo un tema de permisos/vinculación de cuenta del lado del proveedor, no de código — reportado de vuelta, pendiente de su respuesta. Liga/CAI (PaymentTypes=41, ver 5.3az) no se ha vuelto a probar en este mensaje, seguir pendiente de confirmación.

**Actualización misma sesión**: el error avanzó de 404 a código **26**, que ni siquiera está documentado en la tabla de errores de `GenerarReferenciaIndi` (IntegracionesReferencias V1.4) — solo existe en la doc de Liga/CAI, con el significado "no vinculado esta escuela/comercio a su integración". Mismo diagnóstico: BusinessID 000002 probablemente sigue sin estar bien vinculado del lado de Cobroscontarjeta.com para este servicio específico. Reportado de vuelta con este detalle, pendiente de respuesta.

### 5.3bb Corrección: BusinessID/SchoolID real de Pagalaescuela es 000002, no 000041
El correo original de credenciales productivas (ver 5.3aw) decía `BusinessID: 000041` para Pagalaescuela — el usuario confirmó que ese dato estaba mal: el valor real es **000002** (mismo IntegrationID, 106, sin cambio). Corregido en `config.php`: `PLE_SCHOOL_ID` ahora es `'000002'`.

Nota para no perder el hilo: ahora `PLE_SCHOOL_ID` (Pagalaescuela/Liga-CAI) y `PDT_BUS_ID_EFECTIVO` (Pagadetodo/Referencias en efectivo, ver 5.3ba) coinciden en el mismo valor `000002` — son constantes independientes para servicios distintos, la coincidencia es real, no un error de copiar/pegar. `PDT_BUS_ID_SPEI`/`PDT_BUS_ID_TC` (SPEI y CLABEs) siguen en `000067`, sin tocar.

Pendiente: volver a probar tanto Liga/CAI (tarjeta) como Referencias en efectivo con estos valores corregidos.

**Actualización misma sesión**: con BusinessID 000002 el error 26 confirmó su significado completo: "Lo sentimos no esta vinculado este comercio a su integración." Ya se probaron DOS valores distintos (000067 y 000002) con el mismo resultado — descarta que sea un tema de qué número usar; el comercio simplemente no está vinculado a `IntegrationID 125` en el sistema de Cobroscontarjeta.com para el servicio de Referencias en efectivo. Escalado de vuelta pidiendo que lo vinculen directamente de su lado. No hay ningún ajuste de código pendiente para este caso — es 100% acción del proveedor.

**Hipótesis nueva (misma sesión)**: `IntegrationID 125` en el payload de efectivo es correcto — Referencias en efectivo es un servicio de Pagadetodo (125), no de Pagalaescuela (106), tal como decía el correo original de credenciales. Pero el `BusinessID 000002` que se está mandando junto con el `125` es el MISMO valor que se confirmó como el BusinessID de Pagalaescuela (que va con `106`, ver 5.3bb) — sospecha razonable: ese `000002` puede estar vinculado únicamente a la integración `106`, no a la `125`, lo que explicaría el error "no vinculado" sin importar qué BusinessID de efectivo se pruebe. Se le pidió al proveedor confirmar explícitamente la pareja IntegrationID+BusinessID correcta para cada servicio por separado (Pagadetodo/125 vs Pagalaescuela/106) antes de seguir probando valores a ciegas.

**Confirmado por escrito (09-sep-2026) — hipótesis correcta**: Pagadetodo (IntegrationID 125) = BusinessID **000067**; Pagalaescuela (IntegrationID 106) = BusinessID **000002**. El `000002` que se había puesto en `PDT_BUS_ID_EFECTIVO` durante el diagnóstico del error 26 en realidad pertenece a Pagalaescuela, no a Pagadetodo — de ahí el "no vinculado" persistente. **Corregido**: `PDT_BUS_ID_EFECTIVO` vuelve a apuntar a `PDT_BUS_ID_SPEI` (`000067`), igual que antes de todo este diagnóstico — el valor original (SPEI/TC/Efectivo comparten BusinessID en Pagadetodo) era el correcto desde el principio. `PLE_SCHOOL_ID` se queda en `000002` (correcto, sin cambio). Pendiente: confirmar con un cobro real que efectivo ya funciona con esta configuración.

**Actualización misma sesión**: con la pareja correcta (125/000067) el error avanzó de 26 ("no vinculado") a **404: "No tiene permiso para generar referencias, por favor comuníquese con los administradores."** — progreso real: la cuenta ya está vinculada, falta que activen el permiso/servicio de Referencias en efectivo específicamente. Escalado de vuelta.

**Explicación real (09-sep-2026)**: no era un permiso faltante — Cobroscontarjeta.com **movió el servicio de Referencias en efectivo de Pagadetodo (125) a Pagalaescuela (106/000002)**. Aplicado en `acciones/generar_referencia_efectivo.php`: el payload ahora manda `IntegrationID => PLE_INT_ID` y `BusinessID => PLE_SCHOOL_ID` (antes `PDT_INT_ID`/`PDT_BUS_ID_EFECTIVO`) — mismas credenciales que ya usa Pago en línea/CAI. La URL del endpoint (`PDT_URL_REFERENCIA`, `pagadetodo.mx/.../GenerarReferenciaIndi`) NO cambió, solo las credenciales enviadas en el payload. Se eliminó la constante `PDT_BUS_ID_EFECTIVO` de `config.php`/`Config.example` por quedar sin uso. Pendiente: confirmar con un cobro real que efectivo ya funciona con este cambio.

**Actualización misma sesión**: con 106/000002 (la pareja que ellos mismos confirmaron para este servicio) volvió el error 26 "no vinculado". Van 4 combinaciones distintas indicadas por el proveedor (125/000067 → 125/000002 → 125/000067 → 106/000002), todas fallando por vinculación o permisos. El código ya refleja exactamente cada indicación que han dado — el problema es enteramente de aprovisionamiento de su lado. Se les pidió que prueben ellos mismos desde su Sandbox/panel antes de dar una combinación más, en vez de seguir iterando a ciegas desde este lado.

**Cierre del ciclo (09-sep-2026)**: el usuario compartió el correo original de Osbel (soporte@cobroscontarjeta.com), asunto "Certificación Paga La Escuela": *"Hemos movido la realización del método de Pago REFERENCIAS a paga la escuela... PAGALAESCUELA Datos REQUERIDOS: IntegrationID 106, BusinessID 000002."* Esto confirma por escrito, de la fuente original, que el código YA está mandando exactamente los valores correctos — no hace falta ningún cambio más de configuración. El error 26 persistente con estos valores confirmados significa que Cobroscontarjeta.com no terminó de aplicar la vinculación de su lado pese a haber avisado que ya lo habían movido. Reescalado directamente a Osbel citando su propio correo, pidiendo que lo verifique en su plataforma.

**Causa raíz real, confirmada por Osbel**: reprodujimos el error en Postman (mismos datos, fuera de nuestro sistema) y Osbel respondió: *"el metodo de pago de Referencia debes usar la plataforma de PagaLaEscuela y estas usando en la llamada a PagaDeTodo"*. O sea: no bastaba con mandar las credenciales de Pagalaescuela (106/000002) — la URL misma a la que se llama también tenía que ser la de Pagalaescuela (`pagalaescuela.mx/Pagalaescuela/...`), no la de Pagadetodo (`pagadetodo.mx/Pagadetodo/...`), que es donde vivía `PDT_URL_REFERENCIA`. Ambas plataformas parecen ser backends separados aunque compartan formato de API. **Corregido**: nueva constante `PLE_URL_REFERENCIA` en `config.php` (= `PLE_HOST_BASE . '/Service/GenerarReferenciaIndi'`, mismo patrón que las URLs de Liga/CAI), y `acciones/generar_referencia_efectivo.php` ahora llama a `PLE_URL_REFERENCIA` en vez de `PDT_URL_REFERENCIA`. `PDT_URL_REFERENCIA` se deja definida sin usar por ahora (este tema ya cambió de dirección varias veces con el proveedor) por si hiciera falta revertir rápido. Pendiente: confirmar con un cobro real que efectivo ya funciona con la URL correcta.

**Un paso más (09-sep-2026)**: con la URL de Pagalaescuela ya no salió el error 26 — avanzó a código **8: "El ID de la escuela es obligatorio."** El payload solo mandaba `BusinessID` (vocabulario de Pagadetodo); el backend de Pagalaescuela espera `SchoolID` (mismo vocabulario que usa Liga/CAI). **Corregido**: `acciones/generar_referencia_efectivo.php` ahora manda ambos campos (`SchoolID` y `BusinessID`, mismo valor `PLE_SCHOOL_ID`) — mismo patrón defensivo que ya existía en `generar_liga.php` para cubrir los dos vocabularios. Pendiente confirmar con un cobro real.

**Un paso más aún — formato de Reference (09-sep-2026)**: con `SchoolID` corregido, avanzó a código **22: "El formato de la referencia es incorrecto"** — el mismo error que Liga/CAI tuvo al pasar a producción (ver 5.3ax/5.3ay), porque esta acción seguía usando un `str_pad(..., 15, ...)` fijo heredado de cuando corría en Pagadetodo. **Primer intento (revertido)**: reusar `construir_referencia_pago()` (la función compartida de `lib/helpers_pagos.php`) — se descartó antes de subir a producción porque esa función checa unicidad contra el valor ACTUAL de `cobros.referencia`, pero en este flujo esa columna se sobreescribe después con la Reference "envuelta" que regresa el proveedor (necesaria para que `webhooks/consulta_referencia.php` y `webhooks/pago_referencia.php` encuentren el cobro cuando el cliente paga en tienda). Un cobro exitoso "olvidaría" su referencia original de 13 dígitos en cuanto se sobreescribe, y como el bloque "pago" de `construir_referencia_pago()` cicla cada ~166 minutos (`time() % 10000`), un cobro futuro del mismo alumno podría recibir exactamente la misma referencia sin que nada lo detectara — colisión real, no hipotética.

**Fix final aplicado**: `$ref` ahora se construye directo del `id` autoincrement del cobro (`str_pad($cobroRow['id'], REFERENCIA_DIGITOS, '0', STR_PAD_LEFT)`) — único de por vida, sin necesitar ningún chequeo contra BD. Se seguerda de inmediato en `cobros.referencia` antes de llamar al proveedor (para dejar rastro si la llamada truena a medias) y se sobreescribe con la Reference envuelta (`$referencia_cct`) solo si la llamada tiene éxito — igual que antes, pero ahora sin el riesgo de colisión. La doc del proveedor (IntegracionesReferencias_V1_4) confirma que repetir la misma referencia en un reintento del mismo cobro es intencional y soportado, así que tampoco hace falta variar el valor entre reintentos. Pendiente confirmar con un cobro real en producción.

### 5.3bc Foto real de alumno + automatización de Caja + módulo de Proveedores/Gastos
Implementación de las tres mejoras pedidas por el usuario tras una investigación previa (ver plan aprobado en la sesión). Verificado con `php -l`/`node --check` en cada archivo tocado o nuevo.

**Infraestructura compartida de subida de archivos (nueva)**: `lib/uploads.php` (función `guardar_archivo_subido()` — valida tamaño, extensión y MIME real vía `finfo`, nunca confía en `$_FILES[...]['type']`), carpeta `uploads/` con su propio `.htaccess` (sirve estáticos, deniega ejecución de código), constantes `UPLOADS_*` en `config.php`/`Config.example`, `.user.ini` para asegurar los límites de subida en Hostinger. Se corrigió `validar_url_imagen()` en `api.php` para aceptar también rutas locales `uploads/...` además de `http(s)://` — sin esto, `views/Alumnos.js` habría roto el guardado de un alumno con foto subida en cuanto se editara cualquier otro campo suyo.

**Foto real del alumno (Portal Familia)**: nueva acción `acciones/subir_foto_cliente.php` (multipart, valida `requerir_familia_propia`/`requerir_escuela_propia` según el rol). `views/components/FichaTecnica.js` ahora usa un `<input type="file">` con preview local para alumno (el tutor sigue usando enlace externo, sin cambios). `PortalFamilia.js` y `Alumnos.js` ajustados para ya no volver a escribir `foto_url` por su cuenta — la subida ya lo hizo.

**Automatización de Caja**: el backend (`crear_cobro.php`, `caja_cerrar.php`) ya soportaba `metodo='Efectivo'` desde antes, nunca conectado — `views/Caja.js` ahora tiene un botón "Efectivo" real con cálculo de cambio, y se corrigió un bug real: la rama `else` que atendía este caso nunca llamaba a `confirmarPago()`, dejando el cobro en `'pendiente'` para siempre si se disparaba. `views/CorteCaja.js` suma desglose de denominaciones MXN (llena "Efectivo contado" solo), nota obligatoria si hay diferencia al cerrar (validado también en `caja_cerrar.php`, no solo en frontend), y un catálogo de conceptos frecuentes para movimientos manuales. `cron_recordatorios.php` tiene una sección nueva (3) que avisa por correo (nunca cierra sola) cajas abiertas hace más de 18h. Fix de seguridad incidental en `caja_cerrar.php`: antes cualquier usuario autenticado podía cerrar la caja de otro; ahora valida rol + ownership (cajero solo la suya, admin solo las de su escuela).

**Módulo nuevo: Proveedores y Gastos** (no existía nada de esto antes — el sistema solo registraba dinero que entra). Migración nueva `migracion_2026_09_09_proveedores_gastos.sql` (tablas `proveedores` y `gastos`, sin `FOREIGN KEY` por consistencia con el resto del esquema, con índices por `escuela_id`/`fecha`/`proveedor_id`) — **pendiente de correr en phpMyAdmin**. Backend: `crear_proveedor.php`/`editar_proveedor.php`/`toggle_proveedor_activo.php` (soft-delete, patrón Productos pero con `requerir_escuela_propia` añadido por ser dinero) + bloque nuevo en `cargar_datos.php`; `listar_gastos.php` (paginado, filtros, regresa `suma_total` del período completo)/`crear_gasto.php`/`editar_gasto.php`/`eliminar_gasto.php` (DELETE duro, nada más referencia un gasto)/`subir_comprobante_gasto.php` (multipart, usa la infraestructura de uploads compartida). Frontend: `controllers/ProveedoresController.js`, `controllers/GastosController.js`, `views/Proveedores.js` (grid de tarjetas, patrón Productos), `views/Gastos.js` (tabla paginada de servidor, patrón Logs/Cobros, con filtros y total del período). Registrado en `NAV_ITEMS`/`renderView`/`mergeScoped`/`cargarDatosDesdeAPI` de `assets/js/app.js`, `models/AppModel.js` y `index.html` — ambas vistas visibles solo para `admin` (mismo criterio que el resto de módulos financieros de escuela).

**Explícitamente fuera de alcance de esta pasada**: un reporte cruzado de ingresos (cobros) vs. egresos (gastos) — el total simple del período en `Gastos.js` cubre lo pedido; cruzarlo contra `Reportes.js`/`Dashboard.js` queda como posible Fase 2.

**Pendiente antes de operar**: correr la migración SQL en phpMyAdmin; probar en vivo cada parte (foto real, cobro en efectivo, cierre de caja con diferencia, alta de proveedor/gasto con comprobante) antes de considerar esto terminado.

### 6. Correo saliente (SMTP) y Cron de recordatorios
- `config.php` ya apunta a `contacto@pagalaescuela.com` (mail.pagalaescuela.com:465, SSL). Solo falta reemplazar `SMTP_PASS` con la contraseña real de esa cuenta.
- ⚠️ `config.php` está versionado en este repo con credenciales reales (y ya se filtró dos veces por estar en un repo público — ver los comentarios "ROTADO" en el archivo). Antes de subir la contraseña SMTP real, considera moverlo a `.gitignore` o a variables de entorno.
- En el panel de Hostinger → **Avanzado → Cron Jobs**, crear un cron diario (ej. todos los días a las 8:00 am) que ejecute:
  ```
  php /home/TU_USUARIO/domains/tudominio.com/public_html/cron_recordatorios.php
  ```
- Revisa `correos_log.txt` (se crea junto a `api.php`) para confirmar que los correos se están enviando.
- El mismo cron ahora también archiva `logs_sistema` (ver 6.1) — no hace falta un segundo cron en Hostinger.

### 6.1 Archivado automático de `logs_sistema`
- `logs_sistema` es la tabla que más rápido crece (registra cada login exitoso, no solo los fallidos). `cron_recordatorios.php` ahora incluye una sección que corre solo el día 1 de cada mes: mueve las filas de más de 180 días a `logs_sistema_archivo` (la crea sola con `CREATE TABLE ... LIKE`, no requiere migración manual) y las borra de la tabla activa.
- No se pierde histórico — solo se saca del camino de `listar_logs` (panel de Logs del superadmin) y de las consultas de rate-limiting de login, que solo miran los últimos minutos.
- Si alguna vez necesitas revisar logs viejos, consúltalos directo en `logs_sistema_archivo` desde phpMyAdmin (no está expuesta en la UI).
- Paginación de `listar_logs` y `listar_cobros`: se optimizó para que a offset alto (páginas muy avanzadas) MySQL no tenga que leer y descartar el ancho completo de cada fila solo para saltarla — primero busca los `id` de la página (solo toca la columna indexada) y después trae esas filas completas. No cambia la API ni el frontend, solo el rendimiento interno.

## Estructura de archivos
```
index.html              ← Entrada principal (no requiere build, carga todo por <script src>)
config.php              ← ⚠ Credenciales (no versionar)
api.php                 ← Router (332 líneas): auth + despacho a acciones/<accion>.php
cron_recordatorios.php  ← Job diario (Hostinger Cron Jobs) — recordatorios + archivado de logs

acciones/               ← Una accion nueva = un archivo nuevo aquí (ya NO un case en api.php)
  crear_cobro.php, listar_usuarios.php, ...  (79 archivos, uno por acción — ver 5.3l)

lib/                    ← Librerías internas (nunca se llaman por URL directa)
  db.php                ←   Conexión PDO
  mailer.php            ←   Cliente SMTP (enviar_correo)
  helpers_pagos.php     ←   recalcular_saldo_pendiente()
  webhook_helpers.php   ←   webhook_log() + webhook_responder() compartidos
  facturapi.php         ←   facturapi_request() — cliente HTTP hacia Facturapi.io

webhooks/               ← Endpoints de pago con URL fija en el Sandbox de Cobroscontarjeta.com/
  webhook_liga.php,     ←   Pagadetodo — mover/renombrar CUALQUIERA de estos 10 requiere
  webhook_spei.php,     ←   actualizar la URL correspondiente en el Sandbox (ver sección 4.1).
  pago_referencia.php,
  cancela_pago_*.php,
  consulta_*.php,
  entregar_*.php

assets/css/main.css     ← Estilos (paleta Pagalaescuela)
assets/js/app.js        ← Router principal React (carga/mezcla datos, sesión)
assets/js/ApiClient.js  ← Cliente HTTP compartido (fetch + auth + logout en 401)
controllers/*.js        ← Un controller por dominio (Auth, Caja, Cliente, Cobro, Productos)
models/AppModel.js      ← Remanente de localStorage (legado, en limpieza — ver 5.3d)
views/*.js              ← Una vista por pantalla (Login, PortalFamilia, Cobros, Escuelas...)
views/components/       ← Componentes compartidos (Badge, Icons, EmojiPicker, ConfirmModal)
views/hooks/            ← Hooks compartidos (usePaginaActual)

migracion_*.sql         ← Migraciones manuales (correr una vez en phpMyAdmin, ver sección 5)
```
